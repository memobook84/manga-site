# THE MANGA STORE 開発進捗報告

## 作業日
2026年2月14日〜2月16日

---

## 完成した機能

### トップページ（index.html）
- 90作品の漫画を6列×15行のグリッドで表示
- 各作品をカラフルな色分けで表示（CSSベース）
- 検索機能（タイトル・著者名で検索可能）
- ヘッダーナビゲーション（New Release・Followへのリンク）

### 作品詳細ページ（detail.html）
- 作品情報の表示（タイトル、著者、出版社、レーベル、ジャンル、発売日、あらすじ）
- 著者リンク（クリックで著者ページへ遷移）
- フォロー機能（ハートアイコンでお気に入り登録、フォロー時に赤く変化）
- 各巻のリスト表示（クリックで巻詳細ページへ）
- 各巻の発売日を自動計算（初回発売日から3ヶ月ごと）

### 巻詳細ページ（volume.html）
- 各巻の詳細情報
- 購入ボタン（楽天・Amazon）
- 作品ページへの戻るリンク

### 著者ページ（author.html）
- 著者の略歴表示
- 代表作品リスト
- その著者の全作品をグリッド表示

### フォローページ（follow.html）
- フォロー中の作品をグリッド表示
- フォロー解除機能
- localStorageでデータ永続化
- 空の状態のメッセージ表示

### 新刊ページ（new-releases.html）
- 最新20作品をリスト形式で表示
- NEW バッジ表示（上位3作品）
- 発売日順にソート
- クリックで作品ページへ遷移

---

## データベース

### manga-data.js
- 90作品の詳細データベース
- 実在する人気漫画タイトルを使用
- 各作品のデータ項目：
  - id, title, author, publisher, label, genre
  - firstReleaseDate, description, totalVolumes, color
- カラーパレット（82色）

### 含まれる主要作品
- ONE PIECE, 鬼滅の刃, 呪術廻戦, SPY×FAMILY, チェンソーマン
- 僕のヒーローアカデミア, ハイキュー!!, 進撃の巨人, HUNTER×HUNTER
- その他、人気漫画84作品

---

## デザイン仕様

### 2/16 UIリデザイン（Barnes & Noble風 → カスタムデザイン）

#### カラースキーム
| 用途 | 色 |
|---|---|
| ヘッダー背景 | `#595959`（ダークグレー） |
| ページ背景 | `#FAF8F5`（温かみのあるクリーム） |
| カード背景 | `#FFFFFF` |
| テキスト（主） | `#2B2B2B` |
| テキスト（副） | `#5A5A5A` |
| リンク色 | `#6cb4ee`（薄い青） |
| ボーダー | `#E5E1DC`（温かいグレー） |
| フォローハート（未） | `#999`（グレー） |
| フォローハート（済） | `#e74c3c`（赤） |
| 楽天ボタン | `#8B6B5C`（ウォームブラウン） |
| Amazonボタン | `#A68A6F`（トープ） |
| NEWバッジ | `#9B6B6B` |

#### フォント
- **ヘッダー（英語）**: Inter（アルファベットのみ）
- **見出し（英語）**: Crimson Pro（アルファベットのみ）
- **日本語・数字**: Noto Sans JP（全て）
- `@font-face` の `unicode-range` でアルファベットのみInter/Crimson Proを適用

#### ヘッダー
- 背景: ダークグレー（`#595959`）、文字: 白
- コンパクトなパディング（`7px`）
- 下線: `1px solid #444`
- ナビリンク: 大文字表記（NEW RELEASE, FOLLOW）
- 検索窓: `Search manga...`（英語）

#### デザイン方針
- シンプルで自然なデザイン
- 枠線や影を削除（AI臭を排除）
- 余白を活用したクリーンなレイアウト
- ホバーエフェクトで適度なインタラクション
- カードに白背景+薄いシャドウ（トップページのグリッド）

### ファビコン
- 惑星アイコン（`favicon.png`）を全ページに設定

---

## 技術仕様

### フロントエンド
- HTML5 / CSS3 / JavaScript (Vanilla JS)
- レスポンシブデザイン対応
- Google Fonts使用（Inter, Playfair Display, Crimson Pro, Noto Sans JP）
- CSS変数（カスタムプロパティ）による一元管理

### データ管理
- 共通データベース（manga-data.js）
- localStorage（フォロー機能）

### 画像表示
- CSSベースのプレースホルダー
- 各作品固有の色で表示
- 外部サービスに依存しない（完全オフライン動作可能）

### サーバー
- Python HTTP Server（開発用）
- ポート: 8000
- URL: http://localhost:8000

---

## ファイル構成

```
manga-site/
├── index.html              # トップページ
├── detail.html             # 作品詳細ページ
├── volume.html             # 巻詳細ページ
├── author.html             # 著者ページ
├── follow.html             # フォローページ
├── new-releases.html       # 新刊ページ
├── style.css               # 共通スタイル（CSS変数定義含む）
├── detail.css              # 作品詳細スタイル
├── volume.css              # 巻詳細スタイル
├── author.css              # 著者ページスタイル
├── follow.css              # フォローページスタイル
├── new-releases.css        # 新刊ページスタイル
├── ranking.html            # ランキングページ
├── ranking.css             # ランキングページスタイル
├── ranking.js              # ランキングページロジック
├── search.css              # 検索ページスタイル
├── search.js               # 検索ページロジック
├── lightning.js            # 雷エフェクト（トップページ）
├── home.html               # ホームページ（メイングリッド）
├── script.js               # ホームページロジック
├── detail.js               # 作品詳細ロジック
├── volume.js               # 巻詳細ロジック
├── author.js               # 著者ページロジック
├── follow.js               # フォローページロジック
├── new-releases.js         # 新刊ページロジック
├── manga-data.js           # 共通データベース（90作品）
├── favicon.png             # ファビコン（惑星アイコン）
├── api-proxy.py            # 楽天API用プロキシ（未使用）
└── 進捗報告.md             # 本ファイル
```

---

## 2/16の作業内容（UIリデザイン）

### 実施した変更
1. **フォントリンク更新** - 全6HTMLファイルにInter, Playfair Display, Crimson Pro追加
2. **CSS変数定義** - style.cssに`:root`でカラー・フォント変数を一元管理
3. **ヘッダーリデザイン** - ダークグレー背景、コンパクト化、英語ナビリンク（大文字）
4. **カラーパレット変更** - 全体をクリーム背景+落ち着いた色調に統一
5. **フォント分離** - `@font-face` unicode-rangeでアルファベットのみInter/Crimson Pro、日本語・数字はNoto Sans JP
6. **リンク色統一** - 全ページのリンクを薄い青（`#6cb4ee`）に変更
7. **フォロー機能** - ハートアイコン、フォロー時に赤く変化
8. **ファビコン設定** - 惑星アイコンを全6ページに設定
9. **枠の削除** - 作品ページ等のカード枠を削除（AI臭排除）
10. **テキスト色修正** - 薄くて見えにくかったテキストを黒に修正

---

## 2/16の作業内容（ナビゲーション改修・サイドバー・UI改善）

### 1. ナビゲーションフロー改修
- **トップ → 作品ページ → 単行本ページ** の3階層ナビゲーションを実装
- `script.js`: クリック遷移をISBNからtitleのみに変更
- `detail.js`: シリーズページとして全面改修（全巻検索・集約表示・巻数ソート）
- `rakuten-adapter.js`: `extractSeriesName()`を共通関数として移動
- `detail.html`: 「発売日」→「巻数」表示に変更

### 2. サイドバー追加
- 出版社（集英社・小学館・講談社）のフィルタリング（カラードット付き）
- ジャンル（少年漫画・少女漫画・青年漫画）のフィルタリング
- Discover（新刊・ランキング・フォロー中）のリンク
- アイコン付きの洗練されたデザイン、トグル式フィルタ

### 3. ヘッダーデザイン刷新
- ロゴ画像を全ページに追加（タイトル左側）
- ファビコンをロゴに差替
- ヘッダー色を `#9C2020`（ダークレッド）に変更
- ナビリンクをアイコン付きピル型ボタンに刷新
- 検索窓を300pxに調整、letter-spacing縮小

### 4. 表紙画像の取得強化
- 3段階フォールバック: 楽天API → Google Books API → デザインプレースホルダー
- プレースホルダー検出を強化（GIF以外のJPG/PNG、noimage等も対応）
- 最新2巻を除外して古い巻からカバー選択（新刊は画像未登録が多いため）
- API取得を10ページ(300件)に拡大、追加カバー検索も30件に拡大

### 5. トップページUI調整
- カード枠（白背景・角丸6px・影・ホバーエフェクト）を採用
- 画像を固定高さ280px + `object-fit:contain` + `object-position:bottom` で下寄せ
- タイトルが横一列に揃うようflex column化
- スケルトンUI 60件対応

### コミット履歴（2/16）
```
36d0209 トップページUI調整: カード枠復活・画像下寄せ・タイトル一列揃え
9b60086 UI改善: サイドバー追加・ヘッダーデザイン刷新・表紙画像取得強化
6fb5968 トップ→作品ページ→単行本ページのナビゲーション改修
```

### 本番URL
https://manga-site-three.vercel.app

---

## 今後の予定・改善案

### 短期的な改善
- [x] 検索機能の強化（ジャンル・出版社での絞り込み） ← サイドバーで実装済み
- [x] ページネーション ← 実装済み
- [x] 楽天ブックスAPI統合 ← 実装済み
- [x] 実際の商品画像の表示 ← 実装済み
- [ ] 各巻の価格を動的に生成（現在は固定）
- [ ] ソート機能（人気順、新着順など）

### 中期的な改善
- [ ] ドメイン取得（themangastore.co.jp）
- [ ] リアルタイムの在庫・価格情報

### 長期的な改善
- [ ] ユーザー登録・ログイン機能
- [ ] レビュー・評価機能
- [ ] おすすめ機能（AI活用）
- [ ] 購入履歴管理

---

## 2/18の作業内容

### 1. 単行本ページ：前後巻スライダー追加
- 単行本ページ（volume.html）に前後の巻へ移動できるナビゲーションボタンを実装
- シリーズの全巻をAPIから取得し、巻数順にソートして前後を特定
- 「前の巻」ボタン / 現在位置表示（例: 4 / 30巻） / 「次の巻」ボタン
- 最初の巻では前ボタン、最後の巻では次ボタンを無効化
- detail.jsからの遷移時にseriesパラメータを付与して連携

### 2. 購入ボタンのカラー変更
- 楽天で購入ボタン: 楽天レッド（`#BF0000`）
- Amazonで購入ボタン: アマゾンオレンジ（`#FF9900`、文字は黒）

### 3. サイドバー出版社の追加
- 既存の3社（集英社・小学館・講談社）に加え、6社を追加表示
- 秋田書店、KADOKAWA、スクウェア・エニックス、白泉社、双葉社、徳間書店
- 取り扱いなしのためリンク機能なし（見た目は同一）
- カラードットを全出版社から削除

### 4. トップページ：売れ筋ランキング表示
- 楽天ブックスAPIから売上順でデータを取得し、シリーズ集約して上位10作品を表示
- 3段目の後（18作品目の後）に1位〜5位を表示
- 6段目の後（36作品目の後）に6位〜10位を表示
- 各作品にランキング番号を表示（カード上部、Noto Sans JP、48px、太字）
- ランキングエリアは白背景、上下に3pxのグレーライン（`#999`）で区切り
- クリックで作品詳細ページへ遷移
- 検索・フィルタ使用時はランキング非表示
- スマホでは3作品表示に縮小（4位・5位を非表示）

### 変更ファイル
- `volume.html` - 前後巻スライダーUI追加
- `volume.js` - シリーズ全巻取得・前後ナビゲーション機能追加
- `volume.css` - スライダーボタンスタイル、購入ボタンカラー変更
- `detail.js` - 巻詳細遷移時にseriesパラメータ追加
- `index.html` - サイドバー出版社追加
- `script.js` - 売れ筋ランキング取得・挿入機能追加
- `style.css` - ランキングセクションスタイル追加

---

## 2/19の作業内容

### 1. ランキング専用ページ新規作成
- `ranking.html` / `ranking.css` / `ranking.js` を新規作成
- 楽天ブックスAPIから売上順30件を取得し、シリーズ集約して一覧表示
- 5列グリッド（既存の`.ranking-*`系CSSを再利用）
- ランキング番号付き、クリックで作品詳細ページへ遷移
- ホーム埋め込み版の3位以降非表示をページ版では解除

### 2. ナビゲーション整備
- 全7ページ（home, new-releases, ranking, follow, detail, volume, author）のヘッダーナビにRankingリンクを追加（トロフィーアイコン）
- ナビ順序を **Home → New Release → Ranking → Follow** に統一（Homeを一番左に配置）
- home.htmlサイドバーのランキングリンクをranking.htmlへの直リンクに変更

### 3. ボトムナビのタブレット対応
- ボトムナビの表示条件を `max-width: 768px` → `max-width: 1024px` に変更
- iPadアプリでもフッターが表示されるように対応

### 4. トップページ改善
- ロゴクリックでhome.htmlへ遷移するように変更
- Canvas雷エフェクト追加（赤い雷が上から落ちてくるアニメーション）
  - 枝分かれ・フリッカー（明滅）・グロー（発光）効果付き
  - デフォルトOFF、右下のボタンを2秒以内に5回クリックで発動（隠しコマンド）
  - ONのときは1回クリックで即OFF
  - 設定はlocalStorageに保存（次回アクセス時も記憶）
  - 検索結果表示時は自動で非表示

### 変更ファイル
- `ranking.html` - ランキング専用ページ（新規）
- `ranking.css` - ランキングページスタイル（新規）
- `ranking.js` - ランキング取得・表示ロジック（新規）
- `lightning.js` - 雷エフェクト（新規）
- `index.html` - 雷Canvas・トグルボタン追加、ロゴリンク変更
- `search.css` - 雷エフェクト・トグルボタンスタイル追加
- `home.html` - ナビ順序変更、サイドバーランキングリンク変更
- `new-releases.html` - ナビ順序変更・Rankingリンク追加
- `follow.html` - ナビ順序変更・Rankingリンク追加
- `detail.html` - ナビ順序変更・Rankingリンク追加
- `volume.html` - ナビ順序変更・Rankingリンク追加
- `author.html` - ナビ順序変更・Rankingリンク追加
- `style.css` - ボトムナビのブレークポイント変更

### コミット履歴（2/19）
```
630a080 ランキングページ追加・ナビ整備・トップページ雷エフェクト
```

---

## 3/4の作業内容

### 1. フォローボタンのデザイン刷新（detail.html）
- Wishlist風のデザインに変更（画像下に中央配置）
- ブックマークアイコン（SVG）+ テキストのシンプルなスタイル
- 背景なし、アイコンとテキストのみのクリーンなデザイン
- 未フォロー時: 緑系（`#2a6d5a`）ブックマークアウトライン + 「Follow」
- フォロー済み: 緑系（`#2a6d5a`）ブックマーク塗りつぶし + 「Following」

### 2. ダークモード時のサイドバー見出し修正（home.html）
- Publisher / Genre / Discover の見出しがダークモードで見えない問題を修正
- `body.dark-mode .sidebar-heading { color: #bbb; }` を追加

### 3. ダークモードトグルスイッチの調整
- サイズを小さく変更（40x22px → 32x18px）
- 角を直角に変更（border-radius: 2px）
- ダークモードON時の色を青（`#4a90d9`）→ 赤（`#D8052E`、ヘッダータイトルと同色）に変更

### 変更ファイル
- `detail.html` - フォローボタンにブックマークSVGアイコン追加
- `detail.css` - フォローボタンをWishlist風デザインに変更
- `detail.js` - フォロー状態のアイコンfill制御を追加
- `style.css` - ダークモードサイドバー見出し色追加、トグルスイッチ調整

---

## 動作確認済み環境

- **OS**: Windows 11 Home 10.0.26200
- **ブラウザ**: Google Chrome（最新版推奨）
- **サーバー**: Python 3.x HTTP Server
- **解像度**: デスクトップ（1400px以上）で最適化

---

## トラブルシューティング

1. **ページが表示されない** → ブラウザのキャッシュをクリア（Ctrl + Shift + Delete）
2. **変更が反映されない** → ハードリフレッシュ（Ctrl + F5）
3. **フォントが表示されない** → インターネット接続を確認（Google Fonts使用）

---

## 作業終了時の状態

- すべての機能が正常に動作
- 楽天ブックスAPI + Google Books APIで実データ表示
- 3階層ナビゲーション（トップ→作品→単行本）完成
- サイドバーフィルタ（出版社・ジャンル・Discover）実装済み
- サイドバーに9社の出版社を表示
- ヘッダーデザイン刷新（ロゴ・ダークレッド・ピル型ナビ）
- 表紙画像の3段階フォールバック実装済み
- 単行本ページに前後巻スライダー実装済み
- 購入ボタンをブランドカラー（楽天レッド・アマゾンオレンジ）に変更
- トップページに売れ筋ランキング（1〜10位）を2箇所に表示
- ランキング専用ページ（ranking.html）を追加
- 全ページのナビにRankingリンク追加、Home→New Release→Ranking→Follow順に統一
- ボトムナビがiPadタブレットでも表示されるよう対応
- トップページにCanvas雷エフェクト（隠しコマンド: 5連タップでON）
- Vercelで本番デプロイ済み

---

---

## 3/7の作業内容

### おすすめ漫画ページ作成
- `recommend.html` / `recommend.css` / `recommend.js` を新規作成
- 5ジャンル（バトル・アクション、スポーツ、ファンタジー、ミステリー・サスペンス、恋愛・ラブコメ）
- 各ジャンル10作品、プレースホルダーレビュー付き
- Amazon Associates審査対策としてのオリジナルコンテンツ
- 全ページにRecommendナビリンク追加

---

## 3/8の作業内容

### 1. 法的ページ追加（Amazon審査対応）
- `privacy.html` — プライバシーポリシー
- `about.html` — 運営者情報・免責事項・アフィリエイト開示・著作権・お問い合わせ
- `legal.css` — 法的ページ共通CSS
- 全ページにフッターリンク（プライバシーポリシー / 運営者情報）追加

### 2. APIキーのハードコード削除
- `api/search.js`, `api/books.js`, `scripts/collect.js` のAPP_IDを`process.env.RAKUTEN_APP_ID`に統一

### 3. フォローボタン修正
- Phosphorアイコンのクラスを`ph-bold`⇔`ph-fill`に切り替えてフォロー時の塗りつぶし表示を復活

### 4. ピックアップ12作品に拡充
- 邪神の弁当屋さん、魔男のイチ、半分姉弟、怪獣を解剖する、おかえり水平線、RIOTを追加

### 5. ヒーローバナー刷新
- ホームページタイトル: 「管理人のおすすめ漫画」→「EDITOR'S PICK」
- フォント: Zilla Slab Bold → Michroma
- 全ページに統一ヒーローバナー追加

---

## 3/9の作業内容

### 1. サイト名変更
- 「COMIC STORE」→「ATLAS COMIC」に全ページ一括変更

### 2. ナビゲーション構成変更
- Pickup / Database / Popular / Favorites の4つに再構成
- Home → Pickup（アイコン: ph-star）
- New Release をナビから非表示
- Ranking → Popular
- Follow → Favorites（アイコン: ph-heart）

### 3. Favoriteボタン改修（detail.html）
- グレー→赤（#e0245e）のカラー切り替え、アイコンをph-heartに変更

### 4. フォント変更
- ヘッダータイトル: Archivo Black 900 → Montserrat 700
- ピックアップ見出し（EDITOR'S PICK）: Zilla Slab → Michroma 48px

---

## 3/10の作業内容

### 1. ページタイトルデザイン刷新（全ページ）
- 全ページのタイトルフォントをMontserratに統一
- タイトルデザインを「ミニマル左寄せ」に変更（24px PC / 18px mobile、border-bottom付き）
- `.page-hero`を全ページで`<main>`の外に移動し、タイトル位置を統一

### 2. ヘッダー調整
- ヘッダーの上下パディングを7px→14pxに拡大

### 3. ピックアップページ改善
- 「ピックアップ」セクション見出しを削除（EDITOR'S PICKタイトルのみで十分）
- ヒーローバナーとコンテンツ間にスペース追加（margin-top: 32px）
- サブタイトルを「漫画好きの管理人が厳選した注目作品とジャンル別ベスト10」に変更

### 4. Databaseページ
- ランキング埋め込みを非表示に設定（`insertRanking = false`）

### 5. Popularページ（ランキング復旧）
- Service Workerキャッシュバージョンをv2→v3に更新
- ranking関連ファイルをSW静的キャッシュに追加

### 6. 管理人紹介ページ新規作成（profile.html）
- プロフィールヘッダー（アイコン、ニックネーム「アトラス」）
- Profile情報（漫画歴、月の読書量、好きなジャンル等）
- Favorite Manga（TOP5、クリックで作品詳細へ遷移）
- About This Site（サイト説明）
- 全ページフッターに「管理人紹介」リンク追加

### 7. CLAUDE.md最適化・TODO管理
- CLAUDE.mdを123行→46行にスリム化
- TODO.md新規作成（構造化タスク管理）
- Anthropic公式ガイドとの差分分析を実施・メモリに保存

### 変更ファイル
- `style.css` — タイトルデザイン変更、ヘッダーパディング変更
- `home-portal.css` — ヒーローバナー・セクション見出しデザイン変更
- `home.html` — サブタイトル変更、ピックアップ見出し削除、フッターにprofileリンク追加
- `database.js` — ランキング埋め込み非表示
- `sw.js` — キャッシュバージョンv3、ranking関連ファイル追加
- `follow.html`, `ranking.html`, `new-releases.html` — page-heroを<main>外に移動、フッター更新
- `follow.css`, `ranking.css`, `new-releases.css` — mainのmargin調整
- `profile.html` — 新規作成（管理人紹介ページ）
- `detail.html`, `volume.html`, `author.html`, `privacy.html`, `about.html` — フッターにprofileリンク追加
- `CLAUDE.md` — スリム化
- `TODO.md` — 新規作成
- `進捗報告.md` — 各日の進捗追記

---

## 3/17の作業内容

### 1. 管理人紹介ページ改修（profile.html）
- 「Favorite Manga」セクション（TOP5リスト）を削除
- ProfileセクションとAbout This Siteセクションの間の余白を拡大（36px → 80px）
- 読む環境を「紙派（単行本コレクター）」→「電子書籍派（スマホ・タブレット）」に変更

### 2. ヘッダーナビゲーションアイコン削除（全13ページ）
- 全ページのヘッダーナビからPhosphorアイコン（`<i>`タグ）を削除
- テキストのみのシンプルなナビゲーションに変更

### 3. ナビリンクホバーエフェクト変更
- 背景色変化（`background: #F0EEEB`）→ 下線スライドインに変更
- 中央から赤い下線（#D8052E、2px）がスライドするアニメーション

### 4. ナビリンク文字サイズ変更
- font-size を 11px → 14px に拡大（視認性向上）

### 5. ブログページデザイン刷新（blog.html）
- カード型レイアウト → ミニマルリスト型に変更
- タイトル + 矢印のみのシンプルな一覧表示
- 説明文（description）・アイコン・日付を削除
- ホバーで右スライド＋タイトル赤変化のインタラクション

### 変更ファイル
- `profile.html` — Favorite Manga削除、余白拡大、読む環境変更
- `style.css` — ナビリンクホバーエフェクト変更、文字サイズ変更
- `blog.js` — リスト型レンダリングに変更、日付・説明文削除
- `blog.css` — ミニマルリスト型CSSに全面書き換え
- 全13 HTMLファイル — ヘッダーナビアイコン削除

---

---

## 3/20の作業内容

### 1. PCヘッダーダーク化
- PC版（769px以上）ヘッダー背景を#111（ダークブラック）に変更
- テキスト・ナビリンクを白に、ホバー時に白アンダーラインアニメーション
- PC版検索バー: 白背景、ダーク文字（#2B2B2B）
- ナビリンク文字サイズ14px、間隔詰め（gap: 0px）

### 2. スプラッシュ画面変更
- ロゴ画像を非表示、タイトルを白Montserrat 700に変更
- 白い発光テキストシャドウ追加

### 3. ピックアップページ再構成
- index.htmlをPickupポータルに再構成（スプラッシュ→ヘッダー→EDITOR'S PICK→pickup-grid）
- タイトル位置を他ページ（Blog, Database等）と統一

### 4. 単行本ページ購入ボタン変更
- 楽天で購入ボタン削除、Amazonボタンのみに
- ボタンを表紙画像の下に配置（画像と同幅）
- デフォルト: 薄グレー（#f0f0f0）、ホバー: オレンジ（#FF9900）

### 5. ヘッダーロゴ・ナビアイコン整理
- ヘッダーロゴ画像を削除（PCではテキストのみ）
- スマホではロゴ（favicon.png）を表示
- ナビリンクのPhosphor Icons全削除
- ナビテキスト統一: Pickup, Blog, DB, Popular, Favs, About Me

### 6. データベースサイドバーアイコン削除
- Publisher/Genre/Discover見出しとリンクからアイコン削除

### 7. トップへ戻るボタン削除
- 全13ページからscroll-top-btnとスクリプトを削除
- 関連CSSも削除

### 8. 検索アイコン色変更
- 検索ボタンアイコンを青→グレー（#999）に変更

### 変更ファイル
- `style.css` — PCヘッダーダーク化、スプラッシュ変更、ナビ調整、検索アイコン色、scroll-top-btn CSS削除
- `index.html` — Pickupポータルに再構成、タイトル位置統一
- `volume.html` — 楽天ボタン削除、Amazonボタン配置変更
- `volume.css` — Amazonボタンスタイル
- `volume.js` — ボタン再追加ロジック
- `database.html` — サイドバーアイコン削除
- `blog.css` — blog-container padding-top追加
- 全13HTMLページ — About Me統一、ヘッダーロゴ追加（スマホ用）、scroll-top-btn削除

---

## 3/21の作業内容

### 1. ブログカードデザイン改善
- カード縦長化（min-height: 360px）
- カテゴリバッジをカード上部に追加（黒ピル型、白文字、Phosphorアイコン付き）
  - ランキング: ph-trophy、おすすめ: ph-star、まとめ: ph-list-bullets、特集: ph-flag-banner、レビュー: ph-pen
- テキストバランス調整（タイトル16px、説明文13.5px・6行表示）
- ランキング系5記事のタイトルに「管理人が選ぶ」後の改行追加

### 2. チェンソーマンレビュー記事追加（ブログ17本目）
- 好きなキャラ: 早川アキ（復讐から守りたいものへの葛藤）
- 印象的なシーン: パワーの車の事故
- 魅力: 残酷な世界観なのに明るい、異常なテンポの良さ、キャラの魅力
- 第1部 > 第2部の率直な感想

### 3. レビュー記事改善（全4記事）
- 全レビュー記事から「読んだきっかけ」セクション削除
- 「まとめ」→「総評」に変更

### 4. 運営者情報更新
- お問い合わせメールアドレスを atlas0075@outlook.com に変更
- アフィリエイトセクション削除（未参加のため）

### 5. プロフィールページ編集
- ナビリンク: About Us → About Me（全13ページ）
- 読む環境: デジタル（電子書籍メイン）に変更
- 管理人コメントセクション削除
- お気に入り漫画セクション削除

### コミット履歴（3/20〜3/21）
```
0c28fc3 チェンソーマンレビュー記事追加、レビュー記事改善、運営者情報更新
499a3eb ブログカードデザイン改善、スマホロゴ追加、トップへ戻るボタン削除
4423dcc UI刷新: PCヘッダーダーク化、ナビ/検索バー改善、購入ボタン変更、プロフィール編集
```

### 変更ファイル
- `blog.css` — カード縦長化、タグエリア・バッジCSS追加、テキストバランス調整
- `blog.js` — カテゴリバッジ生成、アイコンマッピング、タイトル改行対応
- `data/blog/posts.json` — チェンソーマンレビュー追加、タイトル改行追加
- `data/blog/chainsaw-man-review.html` — 新規作成
- `data/blog/kimetsu-review.html` — きっかけ削除、まとめ→総評
- `data/blog/haikyuu-review.html` — きっかけ削除、まとめ→総評
- `data/blog/fullmetal-alchemist-review.html` — きっかけ削除、まとめ→総評
- `about.html` — メールアドレス更新、アフィリエイト削除
- `profile.html` — コメント/お気に入り削除

---

## 3/24の作業内容

### 1. ボット対策（robots.txt強化）
- Google / Bingのみ全ページ許可
- SEOクローラーをブロック: AhrefsBot, SemrushBot, MJ12bot, DotBot, BLEXBot
- AIクローラーをブロック: GPTBot, CCBot, ClaudeBot, anthropic-ai
- その他ボットをブロック: PetalBot, YandexBot, Bytespider, FacebookBot, Amazonbot
- 未知ボットには `/api/`, `/data/`, `/scripts/` を非公開 + Crawl-delay: 10

### 2. キャッシュ時間延長（vercel.json）
- `/api/*`: s-maxage 300秒(5分) → 86400秒(24時間)、stale-while-revalidate 600秒 → 3600秒(1時間)
- `/data/*`: s-maxage 300秒(5分) → 86400秒(24時間)、stale-while-revalidate 60秒 → 3600秒(1時間)

### 備考
- Vercelフェアユースリミット超過によりデプロイ未実施（制限解除後に再デプロイ予定）
- 将来的な追加対策としてCloudflare（無料）導入を検討中（独自ドメイン取得時に実施予定）

### 変更ファイル
- `robots.txt` — 不要クローラーのブロックルール追加
- `vercel.json` — APIとデータのキャッシュ時間を24時間に延長

---

**作成日**: 2026年2月14日
**最終更新**: 2026年3月24日
**開発者**: Claude Opus 4.6 + memob様

## 2026-06-12 PCヘッダーのちらつき修正
- 原因1: `@phosphor-icons/web` をhead内で同期読み込みしていたため、ページ遷移ごとに描画がブロックされ白フラッシュが発生（バージョン未指定のunpkg URLはCDNキャッシュ約10分のため頻繁に再取得）
- 原因2: `logo.png` が1254×1254px・約1MBで、36px表示なのにデコードが重かった
- 対応: 全15ページのphosphorスクリプトを `defer` + バージョン固定（@2.1.2）に変更、logo.pngを144pxに縮小（993KB→22KB）、style.cssにPC向けクロスドキュメントView Transition（ヘッダーをsite-headerとして固定）を追加、sw.jsキャッシュをv13に更新

## 2026-06-12 ナビホバーの日本語ロール演出を削除
- ヘッダーナビのホバー時に英語→日本語へロール切替する演出を削除（nav-menu.jsのラベル組み替え処理、style.cssのnav-roll関連CSS）
- 理由: 英語で統一したヘッダーの世界観と合わない・日本語ラベルの幅にリンク幅が引っ張られナビ間隔が不均等になるため

## 2026-06-12 巻スワイプの向きを右綴じ漫画準拠に反転
- volume.htmlのスワイプ巻移動を反転: 左→右スワイプ＝次の巻、右→左スワイプ＝前の巻（従来は逆）
- 入場スライドの向き・端の抵抗判定・スワイプ矢印の左右配置（左端＝次、右端＝前）も合わせて反転
- volume.cssの矢印クラスを位置基準の swipe-arrow-left / swipe-arrow-right に改名

## 2026-06-13 シリーズ一覧にNetflix風クイックビューを追加
- detail.htmlのシリーズ一覧で、巻カバーにマウスを置くと右下に丸いキャレットボタンが出現
- ボタンクリックで簡易ポップアップを表示: 表紙・巻タイトル・発売日・価格・レーベル・あらすじ（6行クランプ）・「この巻のページへ」ボタン
- 閉じる: ×ボタン / オーバーレイクリック / Escキー。表示中は左スワイプ遷移を抑止
- タッチ端末（hover: none）ではボタン非表示、従来どおりタップで巻ページへ

## 2026-06-13 クイックビューのデザイン刷新＋モバイルスワイプ対応
- デザイン刷新: 赤い背表紙ライン・生成り台紙のカバーパネル・VOL.タグ・Michromaの「QUICK VIEW」見出し・罫線区切りの英字スペック表・黒→赤の「VIEW VOLUME」ボタン
- モバイル対応: タッチ端末では丸ボタンを常時表示し、タップでポップアップを開けるように変更
- ポップアップ内の左右スワイプで前後の巻に切替（右スワイプ＝次の巻、左スワイプ＝前の巻。巻ページと同じ右綴じ向き）。スライドアウト→スライドインのアニメーション付き、端の巻では引っ張り抵抗
- 巻位置カウンター（3 / 12）とタッチ端末限定のスワイプヒントを追加

## 2026-06-13 スワイプ向きの修正
- 「右にスワイプ＝次の巻」は“指を右に動かす”ではなく“ページが右に進む（指は右→左）”の意味だったため、volume.htmlの巻スワイプとクイックビューのスワイプを反転し直した
- 指を右→左に動かす＝次の巻、左→右に動かす＝前の巻。矢印の配置も復元（左端＝前、右端＝次）

## 2026-06-13 クイックビューの背面スクロール固定＋紫テーマ化
- ポップアップ表示中、オーバーレイのtouchmoveをpreventDefaultし背面ページがスクロールしないよう修正（iOS対策）
- アクセントカラーを赤からテーマカラーの紫 #4B2C82 に変更: 背表紙ライン・QUICK VIEW見出し＋罫線・VOL.タグ・VIEW VOLUMEボタン（ホバー #371F63）・ホバー丸ボタン

## 2026-06-13 モバイルのページ遷移矢印を三角形アイコン化
- detail.html（戻る/全巻一覧へ）と series-volumes.html（戻る）の丸ボタン型矢印を、装飾なしの小さい三角形アイコン（ph-fill ph-caret）に変更
- 白丸背景・枠線・影を除去し半透明の黒三角のみに。タップ領域は40pxのまま維持

## 2026-06-13 クイックビューを下スワイプで閉じる対応
- ポップアップを下方向にスワイプすると指に追従して下がり、90px超で離すと下にスライドアウトして閉じる
- 途中で離すとバウンスして元の位置に戻る。左右スワイプ（巻切替）とは判定を分離

## 2026-06-13 ホームをDatabaseページに変更
- ルート「/」を database.html へリダイレクト（vercel.json の redirects）
- PWAの start_url も /database.html に変更（再インストールまたはアプリ更新後に反映）

## 2026-06-13 ホーム変更に伴いスプラッシュをdatabase.htmlにも追加
- ホームがdatabase.htmlになったため、index.htmlと同じスプラッシュ（ATLAS COMICグリッチ演出）をdatabase.htmlにも設置
- sessionStorage制御は共通なので同一セッションで二重表示はされない

## 2026-06-13 ホームを home.html に正式化（Databaseカタログをホーム化）
- これまで「/」は database.html へリダイレクトする実質ホーム構成だった。これを正式なホーム home.html に整理。
- home.html ＝ Database カタログの内容（正式トップ）。menu.html ＝ 旧 home.html のメニューを移設。
- database.html はリダイレクトスタブ化（meta refresh + JS、query/hash 保持）。vercel.json に /database.html→/home.html を追加、「/」→/home.html に変更。
- manifest.json の start_url を /home.html に。sw.js プリキャッシュを /home.html・/menu.html に更新（CACHE_NAME v13→v14）。
- 全ページのナビ張り替え: ロゴ/タイトル/BASE/ボトムナビ「Database」→ home.html、ボトムナビ「Menu」→ menu.html。検索リダイレクト（index/blog/blog-post）と detail.js/series-volumes.js のフォールバック、nav-menu.js も home.html に統一。
- swipe-back.js の noButtonPages を home.html/menu.html/follow.html に更新。
- CLAUDE.md 構成ファイル・技術メモを更新。

## 2026-06-19 PCメニュー＆スマホヘッダーをPCヘッダーのテーマカラーに統一
- PCヘッダーのテーマカラー `--color-header: #422476` に合わせて以下を `#4B2C82` → `#422476` に変更
  - PCメニューポップアップ `#navMenuPopup` の背景
  - `.nav-popup-list li a:hover` のテキスト色
  - スマホ（max-width:768px）ヘッダーの背景色
- 対象ファイル: style.css

## 2026-08-04 UI全面調整（ヘッダー／メニュー／ページネーション／サイドバー／クイックビュー）

### スマホヘッダー
- 左のワードマーク（logo-header.png）をPWAアプリアイコン（icon-192.png・32px角丸8px）に変更
- 検索を収納型に。虫眼鏡アイコン（Ionicons4 ios-search を nav-menu.js でインライン埋め込み）だけを置き、タップでヘッダー直下に検索パネルが開く。`.search-box` のDOMは動かさずCSSで位置だけ変えているので既存の検索JSは無改修。背面暗転／Esc／スクロール／検索実行で閉じる
- 上下パディング 14px→7px（ヘッダー高 約66px→48px）
- 入力欄を16pxにしてiOSのフォーカス時自動ズームを防止

### スマホのメニュー
- ボトムナビ「Menu」は menu.html へ遷移せず、その場でリスト型ポップアップを開くように変更（nav-menu.js）。暗転＋白カード＋右下「✕ Close」、現在ページはテーマ紫で強調、背面スクロール固定

### PCヘッダー／メニュー
- ナビリンクの色をサイトタイトルと同じ #F7F7F7 に統一
- タイトルのエフェクトを弱める（縁取り 2px→1px、ずらしたベタ影を削除）
- ホーム専用だったアイコン型ナビ（実験ブロック）を撤去し、全ページ共通のテキストリンクに統一
- メニューボタンのアイコンを FontAwesome6 square-caret-down のインラインSVGに（全16ページ）
- メニューポップアップを本棚風カードから縦並びのシンプルなドロップダウンに刷新。半透明＋backdrop-filter、角丸なし、ボタン位置に合わせた三角（位置はJSが --arrow-right で算出）、Browse/Info を区切り線で分割
- ナビリンクを日本語表記に（ピックアップ／ブログ／ブックマーク）
- 検索バーの高さを40px固定にして隣のナビと高さをそろえる

### 表記・アイコンの統一（Favorite → Bookmark）
- Favs/Favorites/Favorite/Favorited を Bookmark 系に統一（全16ページ＋detail.js）
- アイコンをハート・蝶から Phosphor bookmark に統一。詳細ページのボタンは未登録=ph-light／登録済=ph-fill

### ホーム（home.html）
- スマホでは「DATABASE」の page-hero を非表示
- 作品タイトルを1行＋省略記号に。あわせてグリッドアイテムに min-width:0 を入れ、nowrapで列幅が崩れる問題を修正
- サイドバー: 上余白を0にして PUBLISHER 見出しを表紙の上端にそろえた／リンク色を青 #2563eb からグレーに／ホバーと選択中は「文字中央に細い線が左から伸びる」表現に変更
- サイドバーに Label（レーベル）カテゴリを追加。database.js に label フィルタを実装（前方一致＋中黒・空白の表記ゆれを正規化）
- サイドバー折りたたみ・6列表示の状態を localStorage から復元しないように変更（常にサイドバーが出た状態で開始）
- 下部の表示設定トグル3つ（表紙の質感／背景色／ダークモード）を profile.html に移設。表紙の質感は未設定ならオン
- ページネーションを刷新（rakuten-adapter.js の renderPagination）。「1 of 22」形式から、現在ページ中心に最大5個の番号ボタン＋前後シェブロンに。枠なし・選択中はテーマ紫の文字＋下線

### 詳細ページ（detail.html）
- シリーズ一覧の作品タイトルを 15px→11px
- クイックビュー: 「Quick View」の文字と装飾ラインを削除、メタ行の罫線を削除（タイトル下の1本は維持）、タイトルを非太字に（Montserrat 400をフォント読み込みに追加）
- 巻表記を「作品名（1巻）」→「作品名（1）」に、表紙左下のタグを VOL.01 から巻位置カウンター（1 / 17）に変更し右上のカウンターは削除
- Amazon購入リンクを VIEW VOLUME の右に追加（getAmazonBuyUrl／アソシエイトタグ付き）。色は表紙パネルと同じクリーム色
- PCでは左下タグと右下ボタン列の上下中心をそろえた

### メニューページ（menu.html）
- カードの配色をカラフルな10色から全巻オフホワイト（紙#F2EFE9／墨#262428／淡墨#A8A29A）に統一。図案の直書き色を全廃し --ink / --ink2 で描画
- カードタイトルの強制改行を廃止（プライバシーポリシー→プライバシー）

## 2026-08-05 検索まわりの整理・カレンダー簡素化・細部の調整

### 検索
- ヘッダーの検索バーが実際に動くのは home.html だけだった（他15ページは置いてあるだけ、index.html も home-portal.js に処理が無く非機能）。nav-menu.js に共通のフォールバックを追加し、全ページから検索できるようにした
- 検索結果をホームに上書き表示するのをやめ、専用ページ `search-results.html` を新設。JSは書かず database.js を再利用（`?search=` を読む処理と `.manga-grid` / `#pagination` への描画が既にあるため）。キーワード無しで直接開かれた場合は home.html へリダイレクト
- 検索結果は「ヒットした巻数の多いシリーズ順」に並べ替え（長期連載＝人気作の近似）。同数はタイトル順で安定化。database.js / script.js の両方に実装
- api/search.js に `sort` パラメータを追加（既定は standard。`?sort=sales` で売れている順も可）
- 検索結果ページにページネーションは置かない（renderPagination は対象要素が無ければ何もしない）
- ※ script.js はどのページからも読み込まれていない死んだファイルであることが判明（CLAUDE.mdの「旧Pickup用JS」の通り）

### 新刊カレンダー
- PC（≥769px）では曜日列合わせ・表紙サムネイルをやめ、1〜31の数字だけを横並びに。新刊がある日だけ紫の太字、今日は輪、選択中は塗り
- 取得範囲が今月以降なので、今月より前には移動できないように制限（前の月ボタンを disabled 化）

### 詳細ページ
- 「ストーリー」「シリーズ一覧」の見出しを非太字に（800→400）
- シリーズ一覧の作品タイトルを 13px→11px
- カート/カートメニューを幾何学的なデザインに（角を立てる・輪郭線からホバーで塗りに反転・スマホは罫線区切り＋ずらした角ばった影）
- カートボタンはメニューを開かず、直接「全巻カートに入れる」を実行するよう変更（メニューは hidden のまま。巻指定モーダルは導線が無くなった）

### PCヘッダー / メニュー
- メニューのアイコンを FaSquareCaretDown → FaAnglesDown → FaChevronDown と変更（最終は FA5 chevron-down のインラインSVG）
- ポップアップの位置ずれを修正: CSSの right は clientWidth 基準なのに window.innerWidth で計算していたため、スクロールバーの幅ぶん左にずれていた
- 三角の位置をボタンの箱ではなくアイコンの実寸から算出。ヘッダーの実際の下端を測って、三角がヘッダーに食い込まない位置に開くよう変更
- パネルの角丸を6pxに、出現アニメーションを0.34秒の減速カーブに

### ホーム
- サイドバーの見出しを日本語化（出版社／ジャンル／レーベル／探す）。フォントを Noto Sans JP、13px に。見出しの右に短い罫線（リンクのホバー線と同じ currentColor 55%）を追加。1pxの罫線が行によって太さが不揃いに見える問題は line-height を奇数(21px)にして整数座標に落として解消

### その他
- スプラッシュを白背景＋テーマ紫の文字に（グリッチ演出はそのまま。白地では screen 合成だとズレが見えないため multiply に変更）
- 各ページの「← トップページに戻る」をアイコンのみ・左寄せに。色を水色からサイト共通のサブ色＋ホバー紫に

## 2026-08-19 「無料で読める漫画」ページを新設（公式サイトへのリンク集）

### 方針
- 出版社が公式に無料公開している漫画を、当サイトで紹介して公式ページへ送客する形にした
- 作品本体（画像・ページ）は一切ホストしない。表紙も自サーバーに保存せず、公式CDNのURLをその場で表示するだけ
- 理由: リンクを張る行為自体は著作権侵害にならないが、画像やページの転載は複製にあたるため。あわせて海賊版サイトへは絶対にリンクしない（リーチサイト規制）
- あらすじは公式のコピーを使わず、すべて自分の言葉で書き起こした

### 追加ファイル
- `free.html` / `free.js` / `free.css` — 一覧ページ本体。配信元タブで絞り込み、カードは別タブで公式ページを開く（rel="noopener noreferrer"）
- `data/free-manga.json` — 作品データ。手動キュレーション。`_readme` に運用ルールを書いてある
- `api/og-image.js` — 公式ページのOGP画像URLを返すAPI。ホワイトリストに載ったホストしか見に行かない（SSRF対策兼、公式サイト限定の担保）。`</head>` まで読んだら打ち切り、リダイレクトは3回まで

### 表紙画像の扱い
- 通常は `data/free-manga.json` の `cover` を使う（初回表示が速い）
- `cover` が空、または画像の読み込みに失敗した場合だけ `/api/og-image` にフォールバック。CDNのURL差し替えで表紙が消えるのを自動で吸収する
- どちらも取れなかったカードは、タイトル文字だけの紫の表紙にする

### 初期データ（13作品・すべてリンクと表紙のHTTP 200を実地確認済み）
- 少年ジャンプ＋（集英社）7作品: ふつうの軽音部 / エクソシストを堕とせない / 大人大戦 / にこにこ元気☆ハッピーちゃん / みことはこ / 七等星をみつけて / 魔寄りの学校
- ジャンプルーキー！（集英社）3作品: これだから脆弱な短命種は！ / 異形頭短編集 / 石の中の天使
- マガポケ（講談社）3作品: 彼女、お借りします / 東京卍リベンジャーズ / 島耕作
- ジャンプ＋は `/series/{id}/latest_free_episode` にリンクしている。常に最新の無料話へ飛ぶ安定URLのため（`/series/{id}` 単体は404）

### 導線
- PCヘッダーのメニューポップアップ、モバイルのメニューポップアップ（nav-menu.js）に「無料で読める漫画」を追加
- menu.html のカードグリッドに追加（`.npc-free`＝見開きの本の図案を style.css に新設）
- sitemap.xml に `/free.html` を追加（scripts/generate-sitemap.js の静的ページ一覧）

### 未対応
- 小学館（サンデーうぇぶり・裏サンデー）の作品は未登録。両サイトともJS描画で初期HTMLに作品情報もOGPも無いため、自動取得ができない。登録する場合は表紙URLを手で入れる必要がある。ホスト自体は api/og-image.js のホワイトリストには入れてある

### 追記: 一覧を公式RSSで毎日自動更新するようにした（同日）

当初の実装は `data/free-manga.json` が手書きの静的データで、新連載が始まっても載らず、連載が終わっても消えなかった。「常に最新なのはリンクの飛び先だけで、一覧自体は更新されない」という指摘を受けて自動収集に切り替えた。

**公式RSSが5本見つかった**（すべて Giga Viewer 系で同じフォーマット。`<description>`＝作品名、`<author>`＝作者、`<enclosure>`＝サムネイル、`<giga:freeTermStartDate>`＝無料開始）
- `shonenjumpplus.com/rss`（集英社）283件
- `tonarinoyj.jp/rss`（集英社）28件
- `comic-days.com/rss`（講談社）309件
- `magcomi.com/rss`（講談社）28件
- `www.sunday-webry.com/rss`（小学館）41件 ← これで前回「未対応」と書いた小学館が埋まった
- RSSが無いのは rookie / pocket.shonenmagazine / urasunday の3つ。ここだけ手動登録のまま

**`scripts/collect-free-manga.js` を追加**
- 5本のRSSを取得し、作品名でグルーピングして最新話だけ残す → 310作品（自動304・手動6）
- 手で書いた `summary` / `tags` は配信元＋作品名で突き合わせて必ず引き継ぐ（自動では原文を書けないため）
- `manual:true` の作品は一切触らない。`urlLock:true` はリンク先URLを、`coverLock:true` は表紙URLを自動更新から守る
- ジャンプ＋の7作品は `urlLock` を立てて `/latest_free_episode` を維持（話ごとのURLより陳腐化しない）
- 1サイト落ちても他は生かす。失敗した配信元は前回のデータをそのまま残す
- 話タイトルの書式が配信元ごとに違うので `episodeLabel()` で整形（「[123話]作品名」→「123話」）

**既存の日次GitHub Actions（build-search-index.yml）に相乗り**
- 検索インデックス構築の後に収集ステップを追加。楽天キー不要
- コミット対象を `data/search-index.json data/free-manga.json` の2つに変更

**ページ側の対応**
- 310作品を一度に描くと重いので24件ずつの「もっと見る」方式に
- カードのメタ欄に最新話と公開日を表示。並び順は最新話の公開日時の新しい順
- 配信元タブが7つになったので横スクロール対応（CSSは元から対応済み）

**検証**: 全310作品からサンプリングした12作品のリンク・表紙がすべてHTTP 200。手書きの7作品はURL・summary・tagsの引き継ぎを確認済み

### 追記: サムネイルをシリーズ表紙に差し替え（同日）

RSSの `<enclosure>`（話サムネイル）をそのまま使っていたが、これは話ごとのコマ絵で、サイズも 840x420 / 540x384 / 989x656 とバラバラだった。`object-fit:cover` で16:9に切り抜いていたため、作品によって絵が大きく欠けていた。

**エピソードページの og:image がシリーズ表紙を指していることを発見**（ジャンプ＋・コミックDAYS・サンデーうぇぶり・MAGCOMIで確認）。collect-free-manga.js がこれを取りに行くようにした。
- 302/304作品でシリーズ表紙に差し替え成功。残り2作品は og:image が無く話サムネイルのまま（次回の実行で自動リトライされる）
- 取得済みの作品は再取得しないので、平常運転では新作ぶんの数件しか取りに行かない（2回目の実行は297件→2件に減ることを確認）

**取り込んだバグと修正**: 最初の実装は `coverKind === 'series'` のラベルだけを引き継いで `cover` はRSSの値で上書きしていたため、2回目の実行で全件が話サムネイルに逆戻りしたうえ、ラベルが series なので再取得もスキップされていた。URLごと引き継ぐように修正し、さらに「seriesと名乗っているが実体は話サムネイル」を掴まないようURL文字列も検証するようにした。冪等性を確認済み。

**縦横比の違いはぼかし背景で吸収**: シリーズ表紙も配信元ごとに比率が違った（ジャンプ＋482x482、となりのYJ 500x500、コミックDAYS 1125x633、MAGCOMI 1320x880）。切り抜くと絵が欠けるので、同じ画像をぼかして背景に敷き、その上に `object-fit:contain` で全体を収める方式にした（`.fc-cover-bg` / `.fc-cover-img`）。どの比率でも枠が埋まり、カードの高さも揃う。

**残る課題（容量）**: 出版社が未圧縮のPNGを配信しているため、表紙1枚の平均が サンデーうぇぶり1405KB / MAGCOMI 726KB と重い（ジャンプ＋107KB、コミックDAYS182KB は軽い）。初期表示24枚の合計で約4.9MB。`loading="lazy"` で実際に落ちるのは画面内のぶんだけだが、根本的に軽くするには自ドメインでリサイズして配信する必要があり、それは相手画像の複製を作ることになるため保留にしている。

## 2026-08-19 作品ページのブックマークボタンをIMDb風のリボンに

表紙の左下に置いていた丸アイコンボタンを、IMDbのウォッチリストと同じ「表紙の左上に引っ掛かるリボン」に変更した。

- 形は `clip-path: polygon(0 0, 100% 0, 100% 100%, 50% 70%, 0 100%)` で裾をV字に切り欠いたしおり型
- 未登録＝半透明の黒（rgba(18,16,22,.72)）＋白い「＋」、登録済＝ゴールド #F5C518 ＋黒いチェック
- ゴールドにしたのは、どんな色の表紙の上でも埋もれないため。テーマ紫にするなら `.follow-button.followed` の background を `var(--color-accent)`、color を `#fff` にするだけ
- ＋とチェックの出し分けはインラインSVGを2つ置いてCSSの `.followed` で切り替え（Phosphorのクラス付け替えをやめたので detail.js から該当処理を削除）
- 追加したときだけリボンが跳ねる `follow-ribbon-pop` を追加。外すときは静かに戻す
- `aria-pressed` を付けて状態を読み上げに伝えるようにした
- 上端基準になったので、表紙の高さが決まる前でも位置が飛ばない（`.is-ready` による出し分けはそのまま残してある）
- 旧デザイン（丸ボタン）は detail.css にコメントで保持。戻すときはコメントを外してリボン版をコメントアウトする

## 2026-08-19 「ブラックジャックによろしく」の作品ページを追加

楽天ブックスのカタログに無い作品（紙の単行本が絶版）なので、通常の収集バッチでは入らない。手動登録の仕組みごと作った。

### データの出どころ（すべて実地確認）
- 巻数・発売年月・ISBN・価格・レーベル … 国立国会図書館サーチ（OpenSearch API）と openBD の突き合わせ。全13巻、講談社モーニングKC、2002年6月〜2006年1月、各533円
- 表紙 … 著者の佐藤秀峰氏が二次利用フリーで公開している公式配布データ（https://densho810.com/free/）から13枚を取り込み、`covers/black-jack-ni-yoroshiku/` に配置
- あらすじ … 公式配布ページの内容をもとに自分の言葉で書き起こし
- 楽天API・Google Books はどちらも表紙を持っていなかった（確認済み）

### ライセンス
2012年9月15日に商用・非商用を問わず二次利用が許諾された作品。複製・公衆送信が事前承諾なく可能で、条件は「ブラックジャックによろしく」「佐藤秀峰」の2点を明記すること。作品ページはタイトルと著者名を必ず出すので条件を満たす。経緯は `covers/black-jack-ni-yoroshiku/README.md` に残した。

### 手動登録の仕組み（重要）
`scripts/build-search-index.js` は楽天APIの結果だけを見て `data/series/*.json` と `data/search-index.json` をまるごと書き直す。そこへ直接足しても次の実行で消えるため、置き場所を分けた。
- `data/manual-series.json` … 手で登録する作品の置き場
- `scripts/apply-manual-series.js` … それを `data/series/` と `data/search-index.json` に流し込む
- GitHub Actions はインデックス構築の「直後」にこれを走らせる（順番が逆だと意味がない）
- ※ ワークフローがコミットするのは `data/search-index.json` と `data/free-manga.json` だけで `data/series/` は含まれない。CIでのバケット再生成は破棄されるので、リポジトリのバケットは手元で流し込んだものが正

### 表紙パスの扱い
`data/series/` と検索インデックスの表紙は楽天CDNの相対パス前提だった。サイト内のパス（`/covers/…`）も通るよう、`series-cache.js` と `search-index.js` の両方で「`http` 始まり または `/` 始まりならそのまま使う」に変更した。

### 確認したこと
- 作品ページ・全巻一覧ページともHTTP 200、表紙13枚すべて配信可能
- 表示値の検証（Nodeで detail.js のデータ解決を再現）: タイトル/著者/出版社/レーベル/ジャンル（青年漫画）/全巻数13/最新刊2006年01月/価格帯533円/表紙13中13
- 巻タイトル「ブラックジャックによろしく（N）」から巻数が13件すべて正しく抽出できること
- 検索インデックスにも1行追加され、既存24,566件が壊れていないこと

## 2026-08-19 漫画ビューアを追加（ブラックジャックによろしく 第1巻）

作者が二次利用フリーで配布しているPDFを、サイト内で読めるビューアにした。まず1巻だけ。

### ライセンスの確認
配布元（https://densho810.com/free/）の条文を確認済み。
- 「商用・非商用の区別なく、事前の承諾を得ることなく無償で複製し公衆送信」が明示的に許諾されている＝Webビューアはこれに該当する
- 条件は「ブラックジャックによろしく」「佐藤秀峰」の2点明記。ビューア下部に常時表示し、配布元へのリンクも置いた
- 禁止事項のうち Kindleストア・違法サイト・政治利用は該当しない。電子書籍ストア配信時の1円以上の縛りも自サイトのビューアには当たらない
- 唯一グレーなのが「書籍の版面を…複製したものやデータ化したもの…再配布を認めません」。自炊スキャンの再配布禁止と読むのが自然で公式配布データは対象外と解釈したが、断定はできない。**公開後1ヶ月以内の利用報告（info@densho810.com）が義務なので、その際に一言確認しておくのが確実**

### 変換（scripts/pdf-to-reader.py）
- 元PDF: 337MB / 209ページ / 1414x2000のJPEG。zip版(343MB)は中身が圧縮済みJPEGなので逆に大きく、圧縮の役には立たない
- 幅と品質を実測して比較した: 1000/q72→50MB、900/q65→36MB、800/q65→27MB。900/q65で文字も網点も潰れないことを目視確認し採用
- グレースケール化はほぼ無意味だった（元が既にモノクロのため）。効いたのは幅と品質のみ
- 埋め込みJPEGの抽出ではなくページ単位のラスタライズにした。209ページ中198ページしか画像を持っておらず、抽出だと11ページ落ちるため
- 結果: **209枚 / 31.5MB / 全ページ900x1273で揃う**
- `--meta-only` を付けるとJSONだけ作り直せる（クレジット修正時に209ページの再変換を待たずに済む）

### ビューア（reader.html / reader.js / reader.css）
- 右綴じ。**画面の左タップ＝次へ / 右タップ＝前へ / 中央＝バーの出し入れ**。キーボードは←が次、→が前
- スワイプは「指を左→右」で次へ（紙面が右へ送られる向き＝紙の右綴じ本と同じ）
- PCは見開き2ページ、スマホは1ページ。横向きスマホは見開きに切り替わる。見開きは1ページ目を単独にして 2-3, 4-5 … と綴じる（紙の本と同じ並び）
- 見開きのON/OFFボタンを上部に置き、選択はlocalStorageで保持
- 背景は #0B0B0D。バーは自動で引っ込み、紙面だけが残る
- 下部スライダーは `dir="rtl"` で右端が1ページ目。読む向きと一致させた
- 読んだ位置をlocalStorageに保存し、次に開いたとき続きから出る
- 初回だけ操作ガイドを出す
- ページ送りロジックは検算済み: 見開き105組・単ページ209組で、**全209ページが重複なく1回ずつ出て、先頭まで戻れる**

### ビューアへの導線は単行本ページに置く
最初は作品ページ（detail.html）にボタンを置いたが、**作品ページはシリーズ全体のページなので特定の巻に紐づくボタンを置くのは誤り**という指摘を受けて撤去し、単行本ページ（volume.html）へ移した。
導線は「作品ページ → シリーズ一覧 → その巻のページ → 読む」という流れになる。
- `data/readers.json`（`apply-manual-series.js` が `data/reader/*.json` を集めて生成）を見て、**いま開いている巻が読める場合だけ**「この巻を無料で読む」を出す
- PCは横長ボタン（試し読みの隣）、スマホは表紙脇の四角いアイコンボタン。どちらも `.js-read-free` で volume.js が一括制御する
- ゴールドにしたのは、購入（外部サイト）や試し読み（Google Books）と役割が違うことを色で分けるため
- 突合はタイトルの正規化キーなので表記ゆれでも当たる
- 巻ごとの出し分けを検算済み: 1巻＝出る / 2巻・13巻＝出ない / 表記ゆれ「ブラック・ジャックによろしく」1巻＝出る / 無関係な作品＝出ない
- 生成はGitHub Actionsでも走るので、巻を足せば自動で導線が付く

### ついでに直したもの
`volume.css` の末尾に閉じかっこ `}` が1つ余分にあった（今回の変更前から存在。git で確認済み）。ブラウザは黙って無視するので実害は出ていなかったが、削除した。

### サイズ
1巻で31.5MB。リポジトリ全体は80MB（.git除く）。13巻すべて入れると約0.4GBになり、その規模ならリポジトリではなく外部ストレージ（Cloudflare R2 など）に移すべき。

### 単行本ページが「作品が見つかりません」になっていた問題を修正
手動登録した作品の巻を開くと、単行本ページ（volume.html）が空になっていた。
原因は、単行本ページが**楽天API と manga-data.js しか見ておらず、data/series/ のキャッシュを参照していなかった**こと。楽天に無い作品はAPIが必ず空を返すので、キャッシュが唯一の供給元なのに繋がっていなかった。作品ページと全巻一覧ページは既にキャッシュを見ていたので、単行本ページだけ取り残されていた形。

- `volume.html` に `series-cache.js` を読み込ませた（rakuten-adapter → search-normalize の後）
- `fetchVolumeFromSeriesCache()` を追加。ISBN優先、無ければ巻数で1巻ぶんを特定する。API → キャッシュ → manga-data.js の順に落とすので、既存の作品の挙動は変わらない
- 前後ナビ・スワイプ移動・下部のシリーズ一覧を作る `setupVolumeSlider()` も同じ問題を持っていたので、作品ページと同じ「キャッシュ優先 → APIフォールバック」に揃えた。既存作品でもAPI呼び出しが最大5回減る

検算済み:
- 巻の特定: ISBN指定 / タイトルの巻数 / volumeNum指定 / series無し のすべてで正しい巻を引ける。存在しない巻はnullを返す
- 前後ナビ: 13巻が1〜13で並び、1巻では「前＝なし」、7巻では前6・次8になる

### ビューアの導線をクイックビューにも追加、ラベルを「読む」に統一
- ボタンの文言を「この巻を無料で読む」→「**読む**」に変更。左にアイコンを置く形で統一
- クイックビュー（シリーズ一覧の巻カバーから開くポップアップ）にも「読む」を追加。View Volume の左に並ぶ
- 設置箇所は3つ: 単行本ページのPC横長ボタン / 単行本ページのスマホ用アイコンボタン / クイックビューのポップアップ
- 色は3か所ともゴールド。購入（外部サイト）や View Volume（情報を見る）と役割が違うことを色で分けている
- `data/readers.json` は巻を切り替えるたびに取りに行かないよう一度だけ読んで使い回す（`readerIndex`）。作品ページを開いた時点で先読みしておくので、ポップアップを開いた瞬間から判定済みの状態になる
- 巻ごとの出し分けを全13巻で検算済み: 1巻だけ「読む」が出て、2〜13巻は出ない。別作品・巻数null・空タイトルでも出ない。表記ゆれ「ブラック・ジャックによろしく」は出る

### ビューアのページ送りをスライドに
薄く沈ませるだけのフェードをやめ、紙面が横に流れる演出にした。
- **進む＝新しい紙面が左から入り、古い紙面が右へ抜ける**（右綴じの本をめくる向き）。戻るときは逆
- 実装は新旧2枚のレイヤーを重ねて同時に動かす方式。`.rd-spread` を `position:absolute; inset:0` にしてレイヤー化し、`translateX` で流す
- トランジションは流している最中だけ `.rd-sliding` を付けて有効化する。常時付けたままだと初回描画やリサイズでも滑ってしまうため
- `render(dir)` の dir で挙動を分けた: +1=進む / -1=戻る / 0=アニメなしで即差し替え
- スライダー操作・見開き切替・リサイズ・初回描画は 0 を渡して滑らせない（つまみを動かすたびに1コマずつ流れると邪魔なため）
- 連打対策として、めくり中に次の操作が来たら滑らせず即差し替える（入力を捨てない）

### ビューア: PCの矢印表示と、めくりの滑らかさ調整
- PCでカーソルを寄せた側に、その向きの矢印を出すようにした。**左＝次へ進む側なので左向き、右＝前へ戻る側なので右向き**。丸い半透明の面に載せ、ホバーで少し内側へ寄る動きを付けた
- 矢印は `pointer-events: none` にして、クリックは下のタップ領域にそのまま通す
- 表示は `(hover: hover) and (pointer: fine)` に限定。タッチ端末では出ない
- 端まで来たらタップ領域を `disabled` にして、矢印も出ないようにした（`:not(:disabled):hover` で制御）。1ページ目では右矢印、最終ページの組では左矢印が消える。見開き・単ページ両方で検算済み
- めくりを 0.26s / cubic-bezier(0.22,0.61,0.36,1) → **0.46s / cubic-bezier(0.16,1,0.3,1)**（easeOutExpo系）に変更。立ち上がりが速く終わりが長く効くカーブで、紙が滑って止まる感じに近づけた
- 秒数はCSSとJSの両方に出るので、`reader.js` の `TURN_MS` と `.rd-spread.rd-sliding` を揃えるようコメントを入れた（後始末のタイミングがずれると前の紙面が残る）

### ビューア: ページ移動時に一瞬暗くなる問題を修正
戻るときに紙面が一瞬消えて背景の黒が見える、という指摘。

原因は `<img>` に寸法属性を付けていなかったこと。`.rd-page` は `height:100dvh; width:auto` なので、**デコードが終わるまで縦横比が分からず width が 0 に潰れる**。その間は紙面（`background:#fff`）ごと描かれないので、背後の `--rd-bg: #0B0B0D` が見えていた。進むときは先読み済みで一瞬だったが、戻る側は先読みしていなかったため目立っていた。

- `data/reader/*.json` の `size`（900x1273・全ページ共通）から `width` / `height` 属性を必ず付けるようにした。縦横比が先に決まるので、読み込み前から白い紙面の箱が出る
- 先読みを前方だけ → **前後両方**に変更。表示中の組の前後4ページずつを取る
- 先読みした Image の参照を配列に持たせた（読み込み中に回収される可能性があるため）。24件を超えたら古い方から捨てる

検算: 1ページ目では2〜5、100-101の見開きでは96〜99と102〜105、最終付近の208-209では204〜207を先読みする

### ビューア: スマホのスワイプを指追従に作り替え
「スマホのスライドが滑らかでない」との指摘。原因は演出の速度ではなく、**指を離すまで何も動かなかった**こと。スワイプ→無反応→アニメ開始、という間があるため操作と表示が繋がって見えていなかった。

- 指に紙面が追従する方式に変更。横方向と判定した時点で前後の紙面を実際に作って左右に控えさせ、`translateX` で3枚まとめて動かす
- 最初の8px の動きで縦横どちらの操作かを確定させ、以後ぶれさせない。横と決まったら `preventDefault` してブラウザのスクロールや戻るジェスチャに渡さない
- 離したときの判定は「画面幅の18%を超える」か「速度0.45px/ms超のフリック」。ゆっくり短いスワイプは戻し、軽く弾いただけでもめくれる
- 行き止まりの向きへは移動量を0.25倍に鈍らせて、端であることを手触りで伝える
- 離した後の残りは 0.2s に短縮（`.rd-sliding-fast`）。指が大半を運んでいるので、タップ時の 0.46s と同じでは間延びする
- スワイプ直後に飛んでくる click でバーが開閉しないよう、400ms 抑制する
- `render()` から表示更新だけを `syncChrome()` に切り出した。スワイプでは紙面を作り直さずレイヤーを差し替えるので、ページ番号・スライダー・タップ領域の更新だけを呼ぶ

検算: 単ページで209ページすべて踏破し先頭まで戻れること、判定しきい値（ゆっくり30px=戻す / ゆっくり80px=めくる / 素早く40px=めくる / 弾いただけ20px=戻す）、端では隣が無い向きに抵抗がかかること

### ビューア: タブレットで紙面が上下に揺れる問題を修正
スワイプしようとすると縦に動いてしまう、との指摘。`touch-action` を一切指定していなかったため、1本指の動きをブラウザ側がスクロール／バウンスとして処理していた。

- `.rd-stage` に `touch-action: pinch-zoom` を指定。**1本指のパン・スクロールはブラウザに渡さず、ピンチでの拡大だけ残す**（細かい描き文字を読むのに必要なため）
- `html` と `body` を `position: fixed; overflow: hidden; overscroll-behavior: none` で固定。`overflow:hidden` だけでは iPad が書類ごとバウンスするので、動く余地そのものを無くした
- JS側でも1本指の `touchmove` は横縦どちらでも `preventDefault` するようにした（`touch-action` を完全には尊重しないブラウザ向けの二重の押さえ）。2本指のときは何もしないのでピンチは効く
- `e.cancelable` を見てから呼ぶ（passive扱いになっている場合にコンソールが警告で埋まるのを避ける）

※ `.reader-body` は `inset:0` と `height:100dvh` を併記している。iOSの `position:fixed` はレイアウトビューポート基準でURLバーぶん高くなるが、height が bottom より優先されるので dvh 側の高さが採用される

### PCヘッダー: 選択中ナビにアンダーバー
現在開いているページのナビリンク（スケジュール/ランキング/ブックマーク）に下線を追加。線はヘッダーの下端にぴったり接するようにした。

- `.header-nav` を `align-self:stretch` ＋ `margin:-14px 0` でヘッダーの高さいっぱいに伸ばし、`header` の `padding:14px 0` を打ち消した。これでリンクの下端＝ヘッダーの下端になる
- 伸ばした分だけ文字が上下に散るので、リンクとメニューボタンに `align-items:center` を当てて元の位置に戻した
- `.nav-link.active::after` を `bottom:0` に置き、`left/right:14px`（=リンクのpadding）で文字幅ぴったりの長さにした。色はナビ文字と同じ `#F7F7F7`、高さ3px
- `.active` は既に `nav-menu.js` がURLから自動付与していたのでHTML側の変更は不要。モバイルは `.header-nav` が `display:none` なので影響なし

### ページ遷移が一瞬白く/暗くフラッシュする問題を修正
PCでヘッダーのナビを押すと、画面が一瞬消えてヘッダーごと暗く沈んでから切り替わる、との指摘。原因は1つではなく4つ重なっていた。

**原因1: 静的ファイルのCache-Controlが全て `max-age=0, must-revalidate`（Vercel既定）**
遷移のたびにCSS・JS約10ファイルすべてがサーバーへ再検証（304）を取りに行く。そのうちCSS全部とhead内のJSはrender-blockingなので、往復が終わるまで最初の描画が始まらない。これが空白時間の本体。

- `vercel.json` にCSS/JS向け `public, max-age=0, must-revalidate, stale-while-revalidate=604800` を追加。`must-revalidate` は残したまま、再検証をバックグラウンドに回して遷移をブロックさせない（SWR非対応ブラウザは従来通りの挙動なので後退しない）
- 画像・フォントは `max-age=86400, stale-while-revalidate=2592000`
- `vercel dev` は headers 設定を適用しないためローカルでは検証できない。Vercelが内部で使う `path-to-regexp` にパターンを直接かけて、狙ったパスだけに当たること／`/api/`・HTMLに誤爆しないことを確認した

**原因2: Service Workerが全リクエスト「ネットワーク優先」**
`sw.js` が別オリジンのGoogleフォント・jsDelivrまで含めて全部を `fetch()` 優先で握っていたため、原因1の往復にSW起動待ちとワーカースレッド経由のオーバーヘッドが上乗せされていた。用途別に書き分けた。

- 静的アセット(CSS/JS/画像/フォント) → キャッシュ優先＋裏で更新（stale-while-revalidate）。遷移時にネットワークを待たない
- ナビゲーション(HTML) → Navigation Preload を有効化。SWがスリープしていても起動を待たずに本体リクエストが飛ぶ
- `/data/` のJSON → ネットワーク優先のまま（新刊・検索インデックスの鮮度を優先）
- `/api/` と別オリジン → SWを通さず素通し
- localhost はSWごと素通し。編集したてのCSS/JSがキャッシュから返る開発中の事故を防ぐ
- `cache.addAll` は1つでも404だとinstallごと失敗するので、個別 `add` ＋ 失敗握りつぶしに変更

**原因3: head が描画をブロックしすぎ**
- head内の `<script src>` に `defer` を付けた（13ファイル分）。`page-hero-toggle.js` だけはページ見出しのチラ見え防止のため同期のまま
- 事前にbodyのインラインscriptを全ページ走査し、head側のグローバルを同期的に呼んでいる箇所が無いことを確認してから適用した
- render-blockingなアイコンCSSを配る `cdn.jsdelivr.net` と `unpkg.com` に `preconnect` を追加（16ファイル）。fonts系には元から付いていたがCDN側には無く、毎回DNS+TLSのコールドスタートを待っていた

**原因4: PC限定のクロスドキュメントView Transition（＝ヘッダーが暗く沈む正体）**
白フラッシュ対策として入れていた `@view-transition{navigation:auto}` ＋ `header{view-transition-name:site-header}` が、むしろ症状を作っていた。PCだけ（min-width:1025px）に効いていたことも「パソコンで起きる」という報告と一致する。

- 遷移のたびに必ず250msのクロスフェードが挟まる＝毎回まばたきして見える
- sticky なヘッダーに `view-transition-name` を付けると、ヘッダーだけがrootとは別グループでフェードされ、重なった瞬間に暗く沈む
- 新しいページの中身はJSが描くのでスナップショット時点では空。「空にフェード→あとから中身がポップイン」になっていた
- ブロックごと廃止。フラッシュの本体は初回描画の遅さだったので、そちらを原因1〜3で潰す方針に変えた

**仕上げ: ナビ先の先読み（Speculation Rules API）**
- `nav-menu.js` に `prerender` ルールを注入。`eagerness:"moderate"` ＝ リンクにホバー/触れた時点で裏側でレンダリングまで終わらせておくので、クリック時は出来上がった画面に差し替わるだけになる
- 対象はヘッダー/ボトム/メニューのナビのみに限定。作品カードまで含めると楽天APIを空振りで叩く回数が増えるため
- 非対応ブラウザ（Safari/Firefox）は `HTMLScriptElement.supports()` で弾かれ、何も起きないだけ
- 先読みの副作用対策として `splash.js` を `document.prerendering` でガードした。裏で走らせると表示されないまま `splashShown` を立ててしまい、本当に開いた時にスプラッシュが出なくなるため、`prerenderingchange` で表に出てから実行する

検算: `node --check` で sw.js / nav-menu.js / splash.js の構文、`vercel.json` のJSONパース、主要8ページが200を返すこと、`path-to-regexp` でheaderのsourceパターン15パスぶんの当たり方を確認
※ sw.js と vercel.json の効果は本番のみ（localhostはSW素通し・`vercel dev` はheaders非適用）

### 選択中ナビの下線を静的指定に変更（ヘッダーのちらつき対策）
下線を `nav-menu.js` の実行時付与に頼っていたが、このファイルは `</body>` 直前で動くため
「下線なしでヘッダーが描かれる → あとから下線が出る」という二段階描画になっていた。
ヘッダーは遷移時に1フレームも動いてはいけないので、静的指定に切り替えた。

- `ranking.html` / `new-releases.html` / `follow.html` のHTMLに直接 `class="nav-link active"` を記述。最初の1フレーム目から下線が確定し、JSは関与しない
- `nav-menu.js` の実行時付与は静的指定の無いページ向けの保険として残した（`classList.add` なので二重でも無害）。役割が変わったのでコメントを追加
- 16ページの `<header>` ブロックをハッシュで突き合わせ、15ページが完全一致することを確認。`profile.html` だけ差分があったが `aria-label` の「検索」/「Search」の違いのみでレイアウトに影響なし
- `html { scrollbar-gutter: stable }` は元から入っており（style.css:108）、スクロールバー有無による横ズレは既に防げていた

※ `rakuten-adapter.js:453` の「下スクロールでヘッダーを隠す」処理は意図した機能であり、
   遷移時のフラッシュとは別件。ユーザー確認済みでそのまま残す

### 初回表示が遅い問題（フォント・画像）
「シークレットウィンドウだとホームの表示がめちゃくちゃ遅い」との指摘。
2回目以降が速いのはキャッシュに隠れていただけで、初回コストは手つかずだった。
推測で直さず、Chromeヘッドレス（puppeteer-core）でキャッシュ無効・毎回まっさらな
ブラウザ文脈にして実測しながら進めた。

**判明したこと1: render-blocking なCSSが823KB**
head のスタイルシートが揃うまでブラウザは1pxも描かない。そこに
Googleフォント489KB + tabler-icons 236KB + bootstrap-icons 98KB がぶら下がっていた。
FCPの実測（3回の中央値）は次の通りで、フォント3本を外すと 964ms → 124ms（87%短縮）。

- 現状 964ms / Googleフォントだけ外す 408ms / アイコン2本だけ外す 664ms / 3本とも外す 124ms

**対処: 参照ゼロのものだけ削除（17ファイル）**
- tabler-icons の `<link>` を全ページから削除。`ti-` を全文検索して**使用0箇所**を確認済み
- Googleフォントから Michroma / Anton / Big Shoulders Display / Zilla Slab を削除（いずれも参照0箇所）
- Crimson Pro も削除。`style.css:10` の `@font-face` が `src: local('Crimson Pro')` で、
  local() はOSインストール済みフォントしか見ない＝ダウンロードしたウェブフォントは
  元から使われていなかった
- ※ CLAUDE.md の「ピックアップ見出し = Michroma」は古い記述だった

**見積もりが外れた点（記録しておく）**
「フォントCSSが489KB→数十KBになる」と見込んだが、実測は 489KB → 479KB で
10KBしか減らなかった。@font-face も 579個 → 556個。理由は Noto Sans JP が
約120個のサブセットに分割されていて大半を占めていたため。削除した5つは
すべて英字専用の小さなファミリーで誤差だった。**効いたのは tabler の236KB削除のほう。**
→ 残る479KBのほぼ全部が Noto Sans JP の4ウェイト（300/400/500/700）。ここが最大の未着手。

**判明したこと2: 表紙画像が表示の2.6倍の解像度**
表示枠は248pxなのに `_ex=640`（1枚59KB）を59枚＝約3.5MB落としていた。
さらに楽天のサムネCDNが **HTTP/1.1**（`negotiated: HTTP/1.1` を確認）なので
1ホスト6接続しか張れず、59枚が6枚ずつ約10波に分かれて順番待ちする。
1枚の所要時間の中央値が12秒という異常値は、大半が自分の順番を待っている時間。

解像度を横取りして書き換えたA/B実測（全画像が揃うまで、各2回の中央値）:

- `_ex=640`（現状）23,166ms / `_ex=480` 14,120ms / `_ex=320` 4,250ms / `_ex=240` 1,450ms

**対処: ホームだけ `_ex=320` に（ユーザー指示でスコープを限定）**
- `rakuten-adapter.js` の `createImageElement()` に第3引数 `imageSize` を追加。
  省略時は従来通り `pickRakutenSize(height)` なので既存の呼び出しは挙動が変わらない
- `database.js` に `homeCoverSize()` を追加し、パスがホームの時だけ 320 を返す。
  ※ database.js は search-results.html とも共有し、body クラスも両方 `page-database` なので
    クラスでは絞れずパス判定にした
- 検証: ホーム `_ex=320 × 59枚` / search-results `_ex=640 × 6枚`（従来通り）を実測で確認

検算: `node --check` で database.js / rakuten-adapter.js、本番HTMLに tabler 0件・
削除ファミリー0件、`createImageElement` の第3引数と `homeCoverSize()` が配信物に入っていること

※ `loading="lazy"` は実質効いていない（59枚すべて読み込まれる）。Chromeの先読み距離が広いため
※ 楽天画像は sw.js が別オリジンを素通しするためSWキャッシュに入らない。
   楽天CDN側の `Cache-Control: max-age=3600` だけが効いている

## 2026-08-22 ホームの表紙をスマホで少し縮小＋タブレットでサイドバー非表示

**1. スマホのホーム表紙をひとまわり小さく（`style.css` スマホ用データベース調整）**
- `body.page-database .db-cover-frame` に `width: 90%` を追加。
  それまで表紙は列幅いっぱい（`max-width:100%`）で、3列がぴったり詰まって見えていた。
  フレーム側を縮めたので影も一緒に追従する。`align-self:center` ＋ タイトルも
  `text-align:center` なので、縮めても中央で揃ったまま
- 白紙カバー（書影なし）を固定 `100px × 150px` から
  `width:100%` ＋ `aspect-ratio: 112/176` に変更。実表紙と同じ判型・同じ幅になり、
  フレームの縮小に自動で追従する。※ `height:150px !important` の `!important` と
  「インラインのheight指定を上書き」というコメントは、`createPlaceholderHtml()` が
  インラインstyleを吐かなくなっていたため既に不要だった。あわせて削除
- `home.html` の `style.css?v=20260617` → `?v=20260822`（キャッシュ対策）

**2. タブレットでサイドバーを非表示（`style.css` 末尾のタブレットブロック）**
- 既存の「タブレット=5列」ブロック `@media (min-width:600px) and (max-width:1024px)` に
  `.main-layout .sidebar { display:none }` を統合。`.main-layout` は flex なので
  `display:none` にすれば 36px の gap も一緒に詰まり、カタログが全幅で並ぶ
- サイドバーが消えると用が無くなる開閉トグル（DATABASE横のキャレット `.page-hero-caret` と
  6列切替の `.page-hero-grid-toggle`）も同時に非表示

**ハマった点: 最初 `and (orientation: portrait)` を付けて効かなかった**
「タブレットの縦向きだけ」を素直に書いたつもりが、CSSの `orientation` は
**デバイスの向きではなくビューポートの縦横比**で決まる。PCでブラウザ幅を狭めて確認すると
幅 < 高さ になりにくく landscape 判定のまま＝発動しない。
条件を外して幅だけの判定にしたら意図通りになった。
→ 副作用として iPad mini 横持ち（1024px）でもサイドバーが消えるが、
  同じ範囲の「5列」ルールが元から orientation 無しだったので挙動としては一貫している

※ 確認は `npx vercel dev`（ポート3000/3001が使用中で3002に自動フォールバック）。
   sw.js は localhost を素通しする実装なので、開発中にSWキャッシュで古いCSSが返る心配はない

## 2026-08-24 PCヘッダーのメニュー刷新／ホーム1ページ目のシャッフル／作品ページに「アニメ」欄

**1. PCヘッダーのメニューボタン（`style.css` ＋ ヘッダーを持つHTML 18枚）**
- 開いた時にアイコンを180°回す演出を廃止。`.nav-menu-btn.open .nav-caret` の
  `transform: rotate(180deg)` と、それ専用だった `transform-origin` / `transition` を削除
- アイコンを CgMenuCheese（css.gg）から **TbMenu2**（Tabler Icons の menu-2）に差し替え。
  等長3本線のパス `M4 6h16` / `M4 12h16` / `M4 18h16` に置換した。
  塗りではなく線で描くアイコンなので、`.nav-caret` を
  `fill:currentColor; stroke:none` → `fill:none; stroke:currentColor; stroke-width:2` に変更
- ドロップダウンを縦積みから **2カラム横並び**に戻した（左＝さがす／右＝サイト情報）。
  `.nav-menu-col-sub` の区切りを横線（`border-top`）から縦線（`border-left`）に。
  ダークモード側の `border-top-color` も `border-left-color` に直した
- メディアクエリは付けていない。`#navMenuBtn` は `.header-nav` の中にあり
  768px以下では `.header-nav` が `display:none` なので、実質PC専用のため
  （スマホのメニューは別実装の `#mobileMenuOverlay`）

**2. ホーム1ページ目の並びをシャッフル（`database.js`）**
- `shuffled()`（Fisher-Yates、元配列は非破壊）を追加し、
  `fetchFromJson()` のフィルタなし分岐で `page === 1` の時だけ通す
- シャッフルするのは **page-1.json の60件の中だけ**。他ページから作品を持ってこないので
  顔ぶれは従来と同一で、2ページ目以降との重複・抜けが起きない
- コストは実測 **0.75マイクロ秒/回**（60fpsの1フレームの約2万分の1）。
  画像は元から `coverCandidates` の抽選で毎回変わっていたので、通信・キャッシュへの影響もなし

**3. 作品ページに「アニメ」欄と Prime Video 導線（`detail.html` / `detail.js` / `detail.css`）**
- `scripts/collect-anime.js`（新規）で AniList からアニメ化情報を収集し
  `data/anime.json`（新規・18KB・220作品）に出力。**アニメ化率は 17.9%（229/1280）**
- メタ欄の価格帯の下に `#manga-anime-item` を追加。既定は `hidden` で、
  索引に載っている作品だけ `detail.js` の `setupAnimeRow()` が出す
- リンクは `amazon.co.jp/s?k=<アニメ名>&i=instant-video&tag=atlascomic-22`。
  Netflixを検討したが、公開APIが2014年に終了していてアフィリエイトも無く、
  AniList経由でリンクが取れたのは40件中1件（3%）だけだったので Prime Video にした

**ハマった点4つ**
- AniList で `Media` を直接引くと、相乗りさせた1件がヒットしないだけで
  **HTTP 404 になり全件が null** になる。`Page(perPage:1)` でラップすると
  空配列が返るだけで済む。レート制限は 30req/分なので10件相乗り×2.2秒間隔で全1280件が約5分
- アニメ本編を「発表年が一番古いもの」で選ぶと、`ONE PIECE`→OVA（1998）、
  `呪術廻戦`→PV（2018）を掴む。**format（TV/ONA優先）→ 人気度順**に変えて解決
- AniList の検索はあいまいで、別作品を拾って誤ったアニメを案内してしまう。
  返ってきた作品名が `normalizeSearchKey` で問い合わせと同じキーになる時だけ採用し、**52件を除外**
- 索引のキーは `normalizeSearchKey(extractSeriesName(title))` で
  detail.js の引き方と完全に一致させる必要がある。`extractSeriesName` は
  rakuten-adapter.js を require すると window/document を触る初期化まで走るので、
  **関数定義のところだけ正規表現で切り出して `new Function` で評価**している
- `.meta-item[hidden]` が効かない。`.meta-item { display:flex }` の方が
  UA既定の `[hidden] { display:none }` より詳細度が高いため。明示的に打ち消す1行を足した

**4. PCのクイックビューを拡大（`detail.css` の `@media (min-width:1025px)`）**
- 幅 880→1040px、表紙パネル 300→356px、表紙画像 232→276px、
  タイトル 22→25px、あらすじ 13.5px/9行 → 14.5px/10行、閉じるボタン 38→40px
- 1024px以下は縦積みレイアウトに切り替わる別ブロックなので無変更

## 2026-08-24 ホームに「新着作品」列を追加（1巻が最近出たシリーズ）

mangapedia のトップのように、新しく出た作品をホームに出したいという要望。
「新刊（`new-releases.html`）＝最近出た**巻**」に対して、こちらは
「最近始まった**作品**」を並べる。作品単位。

**1. 既存データが使えなかった理由（`firstReleaseDate` の罠）**
- `data/manga-all.json` / `data/pages/page-{N}.json` の `firstReleaseDate` は
  名前に反して**1巻の発売日ではない**。`scripts/collect.js` の `buildSeries()` が
  「最新2巻を除いたプールからランダムに選んだ代表巻」の発売日を入れている
  （`representative = pool[Math.floor(Math.random() * pool.length)]`）
- なのでこれで降順ソートすると、**たまたま代表巻が新しいシリーズ**が並ぶだけになる。
  実際に試すと上位が全部「1巻もの」で埋まった（1巻しかないシリーズは
  代表巻＝その1巻なので日付が正しくなる、というだけの理由）
- 一方 `data/series/*.json`（`build-search-index.js` が焼くシャード）は
  **全巻の実発売日**を持っている（`v[i][3]`）。こちらを使う

**2. `scripts/build-new-series.js`（新規）→ `data/new-series.json`（24件・3KB）**
- APIは1回も叩かない。`build-search-index.js` が集めた巻データを使い回すだけ
- 1巻の発売日＝**巻番号1ではなく発売日の最小値**。番外編・上下巻など
  巻番号が取れない巻があるため
- 除外ルール
  - 出版社は集英社・小学館・講談社のみ（ホームのカタログと同基準）
  - **愛蔵版・新装版・完全版・分冊版など再刊系を title で除外**。これを入れないと
    1巻の発売日が最近になるので「地球へ… 永久愛蔵版」「［定本］漂流教室」
    「史上最強の弟子ケンイチ 愛蔵版」が新着の先頭を占める
  - 1巻が未発売のもの（発売予定）は新刊ページの担当なので出さない
  - 表紙は「実カバーがある一番古い巻」＝なるべく1巻のもの
- 呼ばれ方が2通りある
  - **CI**: `build-search-index.js` の `main()` から `writeNewSeries()` を直接呼ぶ。
    メモリ上の全シリーズを渡すので**1巻しか出ていない新連載も拾える**
  - **手動**: `node scripts/build-new-series.js` で `data/series/*.json` から作る。
    シャードは2巻以上しか焼かれない（`writeSeriesFiles()` の `volumes.length < 2` で return）
    ので1巻ものが落ちる。件数が少ない時はまずこれを疑う

**3. ワークフローのコミット対象に追加（`.github/workflows/build-search-index.yml`）**
- **`data/series/` は CI で再生成されているのにコミットされていなかった**
  （commit 対象が `data/search-index.json` と `data/free-manga.json` だけだった）。
  リポジトリ内のシャードは `u: 2026-08-09` のまま止まっていた
- `data/new-series.json` を diff チェックと `git add` の両方に足した。
  これで毎日 JST 4:00 に自動更新される

**4. フロント（`home.html` / `database.js` / `style.css`）**
- `<main>` のグリッド直前に `#new-series` を追加。既定は `hidden`
- `updateNewSeries()` が出し入れを判断。出すのは
  **ホーム（`/` か `/home.html`）・1ページ目・フィルタなし・検索なし**の時だけ。
  `database.js` は `search-results.html` とも共有しているのでパスで判定する
- 横1列・横スクロール（`overflow-x:auto` ＋ `scroll-snap`）。件数が増えても縦に伸びない
- 表紙は既存の `.db-cover-frame` を流用してカタログと質感をそろえ、
  高さ上限だけ PC 186px / スマホ 152px に絞った
- スマホは `margin: 0 -16px` で `.main-layout` の左右パディング（24px 16px）を
  食い込ませ、端まで流れて見えるようにしている

**5. `sw.js` の `VERSION` を v18 → v19**
- 静的アセットは stale-while-revalidate なので、上げないと
  **新しいHTML＋古いCSS**（＝スタイルの当たっていない新着列）が初回表示される

## 2026-08-24 新着作品を矢印ボタン送りに／サイドバーありでも6列（PCのみ）

**1. 新着作品の送り方をスクロールバー → 左右の矢印ボタンに**
- `.new-series-row` のスクロールバーを消した（`scrollbar-width:none` ＋
  `::-webkit-scrollbar{display:none}`）。`scroll-behavior:smooth` を足して
  ボタン送りが滑らかに動くようにしている
- `.new-series-viewport`（`position:relative`）で列を包み、その左右の端に
  丸ボタンを `left/right: -17px` で半分だけ乗せた
- ボタンの縦位置は列の中央（`top:50%`）ではなく **`top:99px`＝表紙の中心**。
  列の中央に置くとタイトル2行＋発売日のぶんだけ下にずれる
  （列の上パディング6px ＋ 表紙186pxの半分）
- 1回の送り幅は「見えている幅」をカード幅の倍数に丸めた値。
  端に半端な1枚が残らない
- 端まで来た側のボタンは `hidden` で引っ込める。判定は小数の誤差があるので1pxの余裕を見る
- **`setupNewSeriesNav()` は `section.hidden = false` の“後”に呼ぶ**。
  hidden のままだと `clientWidth`/`scrollWidth` が0になり、
  「はみ出していない＝ボタン不要」と誤判定してボタンが出ない
- 表紙の読み込みで列幅が変わるので、`img` の `load` でも測り直す
- **スマホはボタンを出さない**（`@media (max-width:768px)` で `display:none`）。
  従来どおり指のスワイプで送る

**2. サイドバーを出したままでも6冊並べる**
- `body.page-database .manga-grid` を `repeat(5, 1fr)` → `repeat(6, 1fr)`
- 1200px以下は既存のメディアクエリで4列→3列に落ちるので、**効くのは1201px以上＝PCだけ**
- 1カラムの実寸は約167px → 約135px
  （1200 − パディング40 − サイドバー192 − gap36 = 932px を6分割）
- なお `body.grid-six`（サイドバーを隠すトグル）も6列なので、
  今はトグルの役目が「列数を増やす」から「サイドバーを畳んで表紙を大きくする」に変わっている

## 2026-08-24 新着作品の仕上げ（ガラス矢印／シャッフル／カタログとの位置そろえ）

**1. 送り矢印をすりガラス風に**
- ホバーで紫（`--color-accent`）に反転していたのをやめ、**色は変えず濃さだけ変える**
- 通常は `rgba(255,255,255,0.38)` ＋ `backdrop-filter: blur(10px) saturate(140%)`、
  ホバーで `0.6`。ダークモード用に `rgba(40,40,40,0.42)` も用意

**2. 並びをランダムに（`database.js`）**
- JSONは発売日の新しい順で焼いてあるが、表示時に `shuffled()` を通す。
  ホーム1ページ目と同じ扱い。日付順に戻すのは1行
- トレードオフ: 横に見えるのは6枚ほどなので、**一番新しい作品が最初の画面に写らないことがある**

**3. 見出しを太字から通常へ**
- `.new-series-title` の `font-weight` を 700 → 400（`h2` の既定が太字なので明示指定）

**4. 表紙の下端がそろっていなかった**
- `.db-cover-frame` は `margin-top:auto` で下寄せなので、**タイトルが1行の作品だけ
  表紙が下にずれて凸凹になる**
- `.new-series-item h3` に `min-height: 2.8em`（`line-height:1.4` × 2行）を入れて
  タイトルの高さを常に2行分に固定して解決

**5. スマホでカタログと左端がそろっていなかった（3回直してようやく解決）**
- 原因は **`.new-series-row` の `margin: 0 -16px` ＋ `padding: 6px 16px 10px`**。
  「`.main-layout` の左右パディングを相殺して画面端まで流す」つもりだったが、
  実際には下のグリッドと左端がそろわなかった。**この細工を撤去**し、
  `margin: 0` / `padding: 6px 0 10px` にして `.manga-grid` とまったく同じ箱にしたら一致した
- 併せて直した2点（どちらもカタログとの食い違い）
  - **列間**: `body.page-database .manga-grid` はスマホで `gap: 24px 14px`＝横14px。
    列を12pxにしていたのでカード幅も `calc((100% - 28px) / 3)` に修正
  - **タブレット幅**: `@media (min-width:600px) and (max-width:1024px)` で
    カタログが4列（`!important`）になるのに、新着は3列ぶんの幅のままだった。
    このブロックより**後ろ**に4列ぶんの指定を追加（前に置くと効かない）
- `.new-series-item h3` にカタログと同じ `padding: 4px 8px 2px 8px` と
  スマホの字送り（11px / 1.35）を入れて、タイトル文字の位置もそろえた
- ※ 引き換えに、右端のカードは画面の端ではなくコンテンツの右端で切れる見た目になった

---

## 2026-08-24 新着作品の矢印送りを滑らかに（PC）

**やったこと**: 矢印を押した時の横送りを、ブラウザ標準の `scrollBy({behavior:'smooth'})`
から **自前の rAF アニメーション**に置き換えた（`database.js` の `setupNewSeriesNav`）。

**なぜ標準がカクついていたか（3つ重なっていた）**
1. **`scrollBy` の smooth はカーブも時間も固定**。距離に関わらず短くて硬く、止まり際が急に切れる
2. **`scroll-snap-type: x proximity` と喧嘩していた**。smooth が止まった直後に
   スナップがもう一度引っぱるので、最後に小さく跳ねる
3. **`sync()` が毎フレーム `scrollWidth` / `clientWidth` を読んでいた**。
   送っている間ずっとレイアウトが走る（read-after-write のスラッシング）

**直した内容**
- `glide(delta)` を追加。1フレームずつ `scrollLeft` を動かす
  - カーブ: 初回は **easeInOutCubic**（そっと動き出してふわっと止まる）。
    送っている途中で押し直された時だけ **easeOutCubic** に切替
    （easeInOutCubic は出だしの速度が0なので、動いている最中に入れ直すとそこで一瞬止まって見える）
  - 時間: `min(880, 420 + 距離 * 0.28)` ms。長く送る時だけ少し長くかける
  - **連打は「現在位置」ではなく「いまの目標位置」から積む**。
    現在位置から積むと、まだ動いているぶんが食われて1回ぶんに満たない量しか進まない
  - 送っている間だけ `scroll-snap-type: none`。終わったら戻す（上の2.の跳ね対策）
  - `prefers-reduced-motion` の時はアニメなしで即座に移動
- `pointerdown` / `touchstart` / `wheel` が来たらアニメ中断（指・ホイールに譲る）
- `scrollWidth - clientWidth` を `maxCache` に持ち、**resize と表紙のload時だけ測り直す**（上の3.）
- ボタンの `hidden` は値が変わる時だけ書く（毎フレームのDOM書き込みを避ける）
- `style.css` の `.new-series-row` から **`scroll-behavior: smooth` を削除**。
  残すとJSの毎フレーム代入がブラウザ側でも再度アニメされて**二重がけ**になり、逆に鈍くなる

**未対応（デザイン変更になるので保留）**
- `.new-series-nav` の `backdrop-filter: blur(10px)` は、**背後がスクロールしている間
  毎フレームぼかしを描き直す**ので合成コストが高い。ここを削る/半径を下げると更に軽くなるが、
  すりガラスの見た目が変わるため今回は触っていない

---

## 2026-08-26 サイドバーをアコーディオン化・新着作品の見出し整理（ホーム）

**やったこと**: `home.html` のサイドバー（レーベル／出版社／ジャンル）を、
参考にしたECサイトの絞り込みパネルと同じ形に作り直した。あわせて
「新着作品」の見出しまわりを整えた。

**サイドバーの作り直し**
- 段を `<div>` + `<h3>` から **`<details>` + `<summary>`** に変更。
  開閉はブラウザ任せで**JSは足していない**。キーボードでも開閉できる
- 見出し右端の **`＋`／`－`** は擬似要素2本（`::before`＝横棒・常時、
  `::after`＝縦棒・閉じている時だけ）。アイコン画像もフォントも使っていない
- 項目は左を一段インデントし、行末に **`›`**（border 2辺を45度回転）。
  幅に収まらない名前は `text-overflow: ellipsis` で `…` に省略
- ホバーは「文字の下に線が左から伸びる」演出をやめ、
  文字が濃くなる＋矢印が2px右に動く形にした
- 選択中はテーマ紫 `var(--color-accent)`（文字と矢印）
- 並び順を **ジャンル → レーベル → 出版社** に変更

**ついでに直したHTMLの作り**
- フィルタは絞り込みでありページ遷移ではないので `<a href="#">` → **`<button type="button">`**。
  URLに `#` が付かなくなった。`database.js` は `.sidebar-link[data-filter]` を
  見ているだけなので**変更不要**でそのまま動く
- 収録対象外の出版社6件（秋田書店・KADOKAWA・スクウェア・エニックス・白泉社・
  双葉社・徳間書店）は、リンクと同じ見た目なのに押せなかった。
  `.sidebar-link-off` を付けて矢印を出さず色を落とし、押せる3件と区別した
- `<nav>` に `aria-label="作品の絞り込み"` を追加

**線まわり（試行の結果、今は全部止めている）**
- 一度は参考画像どおり左右の縦線＋段ごとの横線で囲む形にしたが、
  最終的に**縦線・横線とも休止**。`style.css` にコメントで残してあるので戻せる
- 縦線を入れていた時に、`.main-layout` が flex のためサイドバーが
  カタログと同じ高さまで引き伸ばされ、縦線がページ下端まで伸びていた。
  **`align-self: flex-start`** で中身の高さに止めた（線を戻す時のために残してある）

**「新着作品」の見出し**
- 以前はアイコン1つ（Material Icons の fiber_new）だったのを**文字に戻した**。
  アイコン版のSVGは `home.html` にコメントで保持
- 字間・フォント・色はサイドバーの見出し（`.sidebar-heading`）に合わせ、
  **大きさだけはサイドバーの項目（13px）**に合わせている
- 一番上の見出し（ジャンル）だけ `padding-top: 0` にして、
  カタログ側の「新着作品」と**同じ高さから始まる**ようにした。
  `＋`／`－` は箱の中央だと文字より下にずれるので、
  `top: 0.7em`（行の高さ 1.4em の半分）で文字の中央に合わせ直している
- 見出しの高さにも横線を追加（`.new-series-header::after`）。
  長さ・太さ・色は下の区切り線（`.new-series::after`）と同じ
  （PC 120px / スマホ 80px）。横位置も下の線とそろえたいので、
  flexの流れから外して `position:absolute` + `left:50%` で**箱の中央**に置いた
  （flexアイテムのままだと見出しの文字幅のぶん中心がずれる）

**その他**
- サイドバー見出しと項目の間隔を 14px → 8px に詰めた
- CSSのコメント開始が `/*` ではなく `\*` になっていた箇所が2つあり、
  そこから次の `}` までが黙って捨てられていた。書き直した際に解消

---

## 2026-08-26 ホームのカタログに「話題の作品」の見出しを追加

ホームの上2段は「新着作品」。その下＝3段目から先のカタログ側に
「話題の作品」の見出しを付けた。**中身（並び・件数・データ）は一切変えていない。**

**追加したもの（`home.html`）**
- `.manga-grid` の直前に `.grid-header` / `.grid-title` を追加。既定は `hidden`

**見た目（`style.css`）**
- 見出しは新着作品のものと同じ部品を共有させた。別々に書くと片方だけ直して
  食い違うので、`.new-series-header, .grid-header` のようにセレクタを並べて1か所で持つ
  - 対象: ヘッダーの flex 指定 / 中央の横線（`::after`）/ 文字の指定（13px・字間0.14em）
- `.grid-header` だけの調整として下余白を PC 16px / スマホ 12px にした。
  新着側は下に横スクロール枠（`padding-top:6px`）が続くぶん 10px で足りるが、
  こちらは表紙がすぐ下に来るのでその 6px ぶんを足している

**休止したもの**
- 新着作品の下の区切り線（`.new-series::after`）をコメントアウト。
  すぐ下の「話題の作品」の見出しが同じ中央の線を持つため、線が2本つづいて重く見える
- 線が持っていた余白のぶん、`.new-series` の `margin-bottom` を
  PC 36px → 56px / スマホ 24px → 44px に増やして間隔を元に近づけた
- 復活させる時は、コメントを外して margin-bottom を 36px / 24px に戻すだけ

**出し入れ（`database.js`）**
- `updateNewSeries()` が新着欄と一緒に `#grid-header` も出し入れするようにした。
  条件は新着欄と同じ「ホーム・1ページ目・フィルタなし・検索なし」
- 2ページ目以降やサイドバーで絞り込んだ時はグリッドの中身が入れ替わるので、
  見出しだけ「話題の作品」のまま残らないようにしてある
  （＝その状態では見出しなし。今までと同じ見た目）
- `search-results.html` も database.js を共有しているが、そちらに `#grid-header` は
  無いので `null` チェックで素通りする

---

## 2026-08-27 見出しのラインを全幅に・新着タイトル1行・サイドバーを段ごとの箱へ

**見出しの横ライン（新着作品／話題の作品）**
- 固定長（PC 120px / スマホ 80px）を絶対配置で箱の中央に置いていたのをやめ、
  flexの子として `flex: 1` で文字の右からメインエリアの右端まで伸ばした。
  幅を指定していないので画面幅・サイドバーの開閉・6列表示にも追従する
- 親が `align-items: baseline` なので、線だけ `align-self: center` で文字の高さの中央に置く
- スマホ側の `width: 80px` は flex-basis に負けて効かないので削除した

**新着作品のタイトルを1行に**
- `.new-series-item h3` の `-webkit-line-clamp` を 2 → 1
- あわせて `min-height` も 2.8em → 1.4em（スマホは line-height が1.35なので 1.35em）。
  表紙は `.db-cover-frame` の `margin-top:auto` で下寄せなので、
  ここに2行分の高さが残ると文字は1行なのに空白だけ居座って表紙との間が開く

**サイドバー**
最終形＝ジャンル / レーベル / 出版社 が**それぞれ独立した箱**。試した順は以下。

1. 休止中だった線を復活（`.sidebar` の左右縦線 ＋ 段の区切り線3種）
2. 縦線 `rgba(0,0,0,0.2)`＝#CCCCCC と 横線 `--color-border`＝#E5E1DC で
   色がバラバラだったので、`--sidebar-line` / `--sidebar-line-w` にまとめて紫に統一。
   `rgba(66,36,118,0.3)`（`--color-accent` #422476 を薄めたもの）＝白地で #C6BDD6
3. 四隅だけ2pxにする案 → 不採用（CSSはコメントで保持）
4. 右の縦1本だけ → 段の区切り線も含めて一旦オフ
5. **四辺すべて囲む**に戻したうえで、段ごとの箱に分割（採用）
   - `.sidebar` からは線を外した。段の間を空けると縦線だけが空白を貫通するため
   - `.sidebar-section` に `border`（四辺）、`.sidebar-nav` に `gap: 10px`
   - 開いている段だけ見出しの下に仕切り線。閉じている段は見出しだけなので引かない

**サイドバーの位置と中身**
- 列ごと右へ10px。`margin-left: 10px` ＋ `margin-right: -10px` で
  幅の増分を打ち消しているので、カタログ側の位置と幅は動かない＝グリッドの列数・列幅はそのまま
- 畳む2状態（`sidebar-collapsed` / `grid-six`）には `margin-left: 0` を追加。
  入れないと畳んだあとに10pxの隙間が居座る
- 中身は左へ8px。見出し 14px→6px / 項目 26px→18px。
  同じ幅ずつ動かしているので階層の段差12pxは保たれる

**一番上の見出し（ジャンル）**
- `padding-top: 0`（＝カタログの「新着作品」と高さをそろえる指定）は外し、
  他の段と同じ扱いに戻した。高さをそろえる必要はないとの判断
- なお「囲む」と「高さをそろえる」は本来両立しない（上に線があれば文字が下がる）。
  両立させるなら箱ごと持ち上げる（`.sidebar` に `margin-top: -17px`）。
  その1行は使う時のために `.sidebar` 内にコメントで残してある

---

## 2026-08-27 サイドバーを枠なしのリスト型に変更

前回の「段ごとの箱」をやめ、枠を使わないデザインに作り直した。

**枠まわり**
- `.sidebar-section` の `border`（四辺）を削除。段の枠は無し
- 区切りは「開いている段の見出しの下」1本だけ
  （`.sidebar-section[open] > .sidebar-heading` の border-bottom）。
  これで生きている `--sidebar-line` は1箇所のみになった
- 段と段の切れ目は `.sidebar-nav` の `gap: 20px` で見せる（枠が無いぶん広めに）

**文字まわり**
- 見出し（ジャンル / レーベル / 出版社）は 12px のまま、色は `--color-text`。
  一度 `--sidebar-arrow` まで淡くしたが濃いままに戻した。
  サイズも一度14pxにしたが12pxに戻した
- ＋／－ の擬似要素は `--color-text-sub` 固定をやめて `currentColor` に。
  見出しの色を変えると一緒に追従する
- 項目は `--color-text-sub` → `--color-text` に濃くした
- 項目の左余白 18px → 6px。見出しと同じ値にして左端をそろえた
  （枠が無いので段差を付けると項目だけずれて見える）
- 項目の上下余白 10px → 12px

**一番上の見出し（ジャンル）**
- `padding-top: 0` を再び有効にして、カタログ側の「新着作品」と同じ高さから始まるようにした。
  `.sidebar` は padding:0 なので、これで文字の上端が列の上端に来る
- 段の枠が無くなったので、以前この方式で問題だった「上の線と文字がくっつく」は起きない
- ＋／－ は `top: 0.7em`（em基準）なので、文字サイズを変えても位置が追従する

---

## 2026-08-29 ブックマーク（PC）をセレクトショップ風のグリッドに

古着屋のECサイトの一覧を参考に、`follow.css` の末尾へ PC 用（561px以上）の
上書きブロックを追加した。スマホ（560px以下）の画像左＋テキスト右の
横並びレイアウトは触っていない。

**カードの下地をやめた**
- `.followed-manga-item` の白背景・影・角丸を全部外して透明に
- 代わりに表紙エリア `.fav-card-cover` を `aspect-ratio: 1/1` の
  正方形タイルにして、内側 8% の余白で表紙を中央に置く
- タイル間は 18px → 10px に詰めた

**キャプション**
- 中央揃えから左揃えへ（`min-width:561px` の中央寄せ指定を打ち消している）
- タイトル 17px → 13px / 行数 4行 → 2行、著者 14px → 12px でグレー #9a9a9a
- 下に 44px の余白を取って、次の段との間を空けた

**背景（ここが一番やり直した）**
- 最初はベタ塗り #f1f0ee。参考画像のグレーは CSS のベタ塗りではなく
  「背景紙で撮った写真そのもの」なので、ベタだと敷いた四角に見える
- そこで ①背景紙のラジアル ②四隅のビネット ③床の落ち込み の3層を重ねたが、
  隅にグレーが溜まって濃く見えた
- 最終形は ②③ を捨てて①だけ。一箇所にグレーを溜めないほうが薄く見える
  `radial-gradient(120% 110% at 48% 34%, #fdfcfc, #faf9f8 60%, #f8f7f6)`
- 濃さはこの3色を動かすだけで調整できる

**影**
- 「背景紙の上に本が置いてある」接地影にしたが、立体感が出すぎたので半分以下に
  - 接地影 `0 18px 28px` / 42% → `0 7px 14px` / 22%
  - 輪郭 `0 3px 8px` / 18% → `0 1px 4px` / 9%
  - ホバー `0 26px 38px` / 46% → `0 11px 18px` / 26%
- 色は黒ではなく暖色寄りの `rgba(38,32,28,…)`。背景紙の色味と馴染ませるため
- `0 0 0 1px rgba(0,0,0,0.035)` の輪郭線だけは残した。
  背景がほぼ白なので、これが無いと白い表紙が背景に溶ける
- 白紙カバー（`.manga-cover-placeholder`）も同じ影・同じ縮尺にそろえてある

**やらなかったこと**
- 参考画像は全幅（画面の端から端）だったので一度そうしたが、
  そこは求められていなかったので撤回。`.follow-main` は 1200px 中央寄せのまま

---

## メニューのポップアップを調整（PC拡大 / スマホをおしゃれに）

### PC：ヘッダーのメニュードロップダウン（`#navMenuPopup`）を一回り大きく
`style.css` の値を上げただけで、構造・配置ロジックは無改修。
- カード内側の余白 `20px 24px` → `26px 32px`、カラム間 gap `28px` → `36px`
- カラム最小幅 `170px` → `205px`、区切り線の左余白 `28px` → `36px`
- 項目の文字 `15px` → `17px`（右カラムのサブ項目は `14px` → `16px`）
- 項目の padding `9px…22px` → `11px…26px`、区切り線の余白も少し広げた
- 見出し（さがす／サイト情報）`11px` → `12px`
- 行頭の三角マーカー `6×10px` → `7×12px`
- しっぽの位置は `nav-menu.js` がボタン中心から実測して `--arrow-right` に入れるので、
  幅が変わってもズレない（だから触っていない）

### スマホ：ボトムナビ Menu のポップアップ（`#mobileMenuOverlay`）
「白いカードにヘアラインのリスト」から、すりガラス基調に作り替えた。

**カード**
- 白ベタ `#fff` → `rgba(255,255,255,0.94)` ＋ `backdrop-filter: blur(24px) saturate(1.4)`
- 角丸 `16px` → `22px`、枠に `1px solid rgba(255,255,255,0.7)`
- 影は3層（落ち影 + 近接影 + 内側の上ハイライト）。紫寄りの `rgba(24,12,45,…)` にして
  背面の暗転と馴染ませている
- 背面 `.mm-backdrop` も `blur(7px)` を足した。これが無いとカードのすりガラスが効かない

**見出し（新規）**
- `nav-menu.js` にカード先頭の `<div class="mm-head"><span>MENU</span></div>` を追加
- Montserrat 11px / `letter-spacing:.22em` の紫。右へ細いグラデ線が伸びる
- `position:sticky` にして、スクロールしても残す。下端を透明へ落として
  行が奥へ流れて見えるようにした

**行**
- 罫線を `border-bottom` から擬似要素 `::after` に変更。左右を空けた短いヘアラインにして軽く見せる
- 左端に `::before` のアクセントバー（紫グラデ 3px）。現在ページの行だけ高さ 22px に伸びる
- 現在ページの背景に紫の横グラデを薄く敷いた
- 文字 `14.5px` → `15px`、押した時は紫の薄い面 + 「›」が 3px 右に動く
- 区切り `.mm-sep` は 7px のグレー帯 → 両端が消える 1px の線

**動き**
- カード全体 `0.24s` → `0.36s` の `cubic-bezier(.16,1,.3,1)`
- 行を 0.03s ずつずらして順に立ち上げる（`a.mm-row:nth-of-type(n)` で指定。
  見出しと区切りは div なので `nth-of-type` の数に入らない）
- `prefers-reduced-motion: reduce` の時は行が消えないよう `opacity:1 / animation:none`

**閉じるボタン**
- 右下の黒い角丸 → カード下中央の磨りガラスのピル（`border-radius:999px`、
  白 12% + `blur(14px)`、Montserrat の大文字 `letter-spacing:.16em`）

---

## スマホのメニューを ClickUp 風のリストに作り替え（前項のすりガラス案は撤回）

参考にもらったスクショ（左アイコン ＋ ラベル ＋ 右シェブロン、罫線はグループ間だけ、
最下段が赤）の形にそろえた。前の項で入れたすりガラス／MENU見出し／左アクセントバーは廃止。

### `nav-menu.js`
- `MENU_ITEMS` を `[パス, ラベル, アイコン名]` の3要素に変更。アイコンは全ページで
  読み込み済みの `@phosphor-icons/web@2.1.2` の regular（`.ph`）を使う
  ホーム=house / 新刊=sparkle / ランキング=trophy / ブックマーク=bookmark-simple /
  フリー漫画=gift / ピックアップ=fire / ブログ=note-pencil / QR=qr-code /
  管理人紹介=user-circle / 運営者情報=info / プライバシー=shield-check
- 区切りを1本追加（さがす系／ピックアップ・ブログ／サイト情報 の3グループ）
- 右端の「›」は文字からアイコン `ph-caret-right` に変更
- 前項で足した `.mm-head`（MENU見出し）は削除
- カード下に置いていた閉じるボタンを、カード内の最終行に移設
  （`<button class="mm-row mm-close">` ＋ `ph-x`）。JS側の `.mm-close` 参照はそのまま動く

### `style.css`
- カード: 半透明＋blur → 白ベタ `#fff` / 角丸 22px → 14px / 影も控えめに
- 背面のぼかしを撤去（フラットな見た目とケンカするため）。暗転のみ `rgba(20,17,28,.44)`
- 1行ごとのヘアラインを廃止。区切りは `.mm-sep` のみで、カードの端から端まで通す1本線 `#EFEEF3`
- 行: `padding:11px 14px 11px 16px` / gap 13px / 文字15px。アイコン欄は `width:20px` 固定で
  ラベルの開始位置をそろえる
- アイコン色は固定色ではなく `currentColor` ＋ `opacity:.68`。
  こうしておくと現在ページ（紫）や閉じる（赤）で自動的に色が付く
- 現在ページ: 左のバーをやめて、行ごと紫 `#4B2C82` ＋ 薄い紫の面。太字
- 閉じる: サイトのアクセント赤 `#D8052E`
- 行のスタッガー演出は残した（0.02秒刻み）。`prefers-reduced-motion` のフォールバックも維持
- ダークモードは触っていないが、閉じるがピル型だった頃の
  `body.dark-mode .mm-close{background:#E9E9EC}` だけは行として破綻するので色指定に直した

---

## スマホ（ブラウザ表示）のナビをヘッダーに集約。ボトムナビとFABを廃止

### 経緯
モバイルにナビの入口が2つあった。
- 右下のFAB（渦巻き）→ タップでボトムナビが縦に扇形展開（5項目）
- ボトムナビの Menu → ポップアップメニュー（12項目）

同じ「ナビを開く」に別の見た目と別の中身が2系統。渦巻き＝ポップアップに一本化した。

### 変更後の挙動
- **スマホ（768px以下）× ブラウザ表示**: ボトムナビは出さない。
  ヘッダー右に「検索🔍 → 渦巻き」の2アイコン。渦巻きでポップアップが開く
- **PWA（standalone）**: 従来どおりボトムナビ。Menu からポップアップ
- **タブレット（769〜1024px）**: FABが無くなったので、ブラウザ／PWAとも
  通常の横並びボトムナビになる（旧仕様ではFABだった）

### `nav-menu.js`
- 収納型ボトムナビのブロック（`.bottom-nav-fab` 生成 + `fab-open` の扇形展開）を全削除
- 開閉処理を `toggleMobileMenu()` に切り出し、ボトムナビのMenuとヘッダーの渦巻きで共用
- ヘッダーに `.header-menu-toggle`（渦巻きSVGは旧FABから移植）を追加。
  ボタンは常に生成し、出す／出さないはCSSのメディアクエリに任せる（検索トグルと同じ方式）
- `.header-container` への append 順が「検索 → 渦巻き」なので、
  検索側の `margin-left:auto` によって2つが右端に寄り、検索が渦巻きのぶん左へずれる

### `style.css`
- FAB関連（`.bottom-nav-fab` と `@media (display-mode: browser) and (max-width:1024px)` 一式）を削除
- `.header-menu-toggle` を追加。検索トグルと同じ34pxの円。
  `.header-container` の `gap:20px` が2連アイコンには広すぎるので `margin-left:-12px` で8px相当に詰めた
- ファイル末尾に切り替え本体を追加:
  `@media (display-mode: browser) and (max-width: 768px)` で
  `.header-menu-toggle{display:inline-flex}` / `.bottom-nav{display:none}` / `body{padding-bottom:0}`
  ※ `.bottom-nav{display:flex}` と `body{padding-bottom:84px}`（max-width:1024px のブロック）を
    上書きするので、必ずファイル末尾に置くこと

### 効いてくる副次効果
- `body { padding-bottom: 84px }` が消えるので縦の表示領域が広がる
- FABが `bottom:18px` にいて iOS Safari の下部ツールバーと干渉していたのが解消

### 未対応（要判断）
- ボトムナビにあった「戻る」ボタンがブラウザ表示で消える。
  ブラウザ自身の戻る＋ swipe-back.js の左→右スワイプで代替できているが、
  ポップアップに「戻る」を足すかは保留

---

## スマホのホーム：セクションを見出しと余白で分ける

「新着作品」と「話題の作品」が地続きに見える、という指摘への対応。
案は3つ出して（①薄い紫の面で分ける ②見出しと余白 ③全幅のグレー帯）、
②の「色も線も増やさない」方向を採用。

### `home.html`
- 各セクションの見出しの先頭に英字ラベルを追加
  - 新着作品 → `<span class="section-eyebrow">NEW ARRIVALS</span>`
  - 話題の作品 → `<span class="section-eyebrow">TRENDING</span>`
- `.grid-header` は database.js が `hidden` を出し入れするだけ（425行あたり）なので、
  子要素を足しても壊れない

### `style.css`
- `.section-eyebrow` は既定 `display:none`。**PCでは出さない**
  （PCはサイドバーがあって元々セクションの区切りが見えているため）
- スマホ（768px以下）だけ:
  - `.new-series` の下余白 44px → **80px**。分離は余白で作る
  - `.new-series-header / .grid-header` に `flex-wrap: wrap` ＋ `row-gap: 3px`
    親は `align-items: baseline` の flex なので、`width:100%` の英字ラベルが
    1行目を占め、和文見出しと右へ伸びる線（`::after`）が2行目に落ちる。
    結果「NEW ARRIVALS / 新着作品 ──────」の2段になる
  - 英字ラベル: Montserrat 10px / 700 / 字間 0.2em / `#9B90AE`
  - 和文見出し: 13px → **15px**、weight 500（ラベルが付いたぶん見出しらしく）

### 触っていないもの
- 見出しの右へ伸びる線（`::after`）はそのまま。2行目に一緒に落ちるだけ
- PCの見た目は完全に据え置き
